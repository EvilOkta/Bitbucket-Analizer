# Changelog (Журнал изменений)

Все важные изменения проекта документируются в этом файле.

## [3.2.0] - 2026-09-01
### Полномасштабное внедрение дизайн-системы Obsidian Metric во все 10 представлений приложения
- **Полная унификация интерфейса под стандарт Obsidian Metric ([DESIGN.md](file:///d:/Вайбкодинг/Bitbucket/DESIGN.md))**:
  - **Обзор стека (`StackOverviewView.tsx`)**: Рефакторинг карточек языков программирования, фреймворков, баз данных и брокеров сообщений. Переход на монохромные графитовые плашки `#161922` с тонкими 1px разделителями `#1E2330` и акцентными бейджами версий на `JetBrains Mono`.
  - **Рекомендации (`RecommendationsView.tsx`)**: Графитовые карточки аудита и подсказок с чистыми бейджами критичности (`CRITICAL`, `WARNING`, `INFO`), плоские кнопки действий и моноширинные сниппеты кода.
  - **Журнал аудита (`AuditLogsView.tsx`)**: Прецизионная таблица событий безопасности с поисковой строкой `#0D0E14`, фильтрами статусов и чистыми временными метками.
  - **Подключения Bitbucket & Confluence (`ConnectionsView.tsx`)**: Стек конфигурационных панелей серверов с recessed-инпутами `#0D0E14`, акцентными индикаторами активного статуса и чистыми кнопками проверки соединения.
  - **Публикация в Confluence (`ConfluencePublishView.tsx`)**: Графитовые карточки пространств и страниц, компактные чекбоксы экспорта разделов и синяя акцентная кнопка публикации `#2563EB`.
  - **Репозитории (`RepositoriesView.tsx`)**: Компактные списки проектов и репозиториев, монохромные выпадающие списки веток и кнопки инициализации анализа.
  - **Проводник кода (`RepoExplorerView.tsx`)**: Темная графитовая файловая структура `#111318`, монохромный редактор кода `#0D0E14` с аккуратной подсветкой активной строки и быстрым переходом к AST-узлам.
  - **Карта API (`ApiMapView.tsx`)**: Контроллеры с компактными спойлерами, высококонтрастные бейджи HTTP-методов (GET/POST/PUT/DELETE/PATCH), плоские DTO-модальные диалоги и чистый поиск.
  - **Модель данных & ERD (`DataModelView.tsx`)**: Компактный список сущностей с классификацией (Таблицы, ENUM, Системные типы), графитовая спецификация колонок и переключений между интерактивным графом D3 и Mermaid ERD.
  - **Граф структуры проектов (`ProjectGraphView.tsx`)**: 4-уровневый холст D3 на фоне `#090A0F`, верхняя панель навигации `#111318`, компактные селекторы и боковой выдвижной инспектор узлов `#111318`.
- **Очистка от устаревших стилей (`glass-panel`)**:
  - Полностью устранены остаточные фрагменты `glass-panel` и размытые градиентные подложки во всех компонентах.
- **Тестирование, компиляция и Portable сборка**:
  - Все 36 модульных тестов пройдены успешно (100% pass, `node --test tests/engine.test.js`).
  - Строгая компиляция TypeScript (`npx tsc --noEmit`) завершена с 0 ошибок.
  - Собрана финальная portable Windows-версия: `dist-package/Bitbucket Architecture Analyzer 1.0.0.exe` (98.1 MB).

## [3.1.0] - 2026-09-01
### Реализация модуля «Автотесты & Test Runner»: анализ тестов, матрица покрытия, генератор кода тестов и встроенный диагностический раннер
- **Анализ тестов репозитория (`testExtractor.ts`)**:
  - Детекция и парсинг тестовых сьютов и тест-кейсов для всех основных экосистем: **Pytest / unittest** (Python), **Vitest / Jest / Playwright / Cypress** (JS/TS), **xUnit / NUnit / MSTest** (.NET C#), **JUnit** (Java/Kotlin), **GTest / Catch2** (C++).
  - Классификация тестов по типам: `unit`, `integration`, `e2e`, `form`, `api`.
  - Автоматический расчет матрицы архитектурного покрытия: процент покрытых API эндпоинтов, UI экранных форм и PostgreSQL моделей данных.
- **Генератор исполняемого кода тестов (Test Synthesizer)**:
  - Генерация готовых исполняемых тестов для любого выбранного API-метода (Pytest HTTPX / Vitest Supertest).
  - Генерация E2E и компонентных тестов для UI экранных форм (Playwright E2E / Vitest Testing Library) с проверкой ввода полей и клика отправки.
  - Подсветка синтаксиса и кнопка мгновенного копирования кода в буфер обмена.
- **Интерактивный Test Runner (Live Engine Diagnostics)**:
  - Запуск встроенного набора из 35+ архитектурных тестов движка прямо из интерфейса.
  - Отображение прогресса, таймингов в миллисекундах на `JetBrains Mono` и статус-точек (`pass`/`fail`).
- **Интерфейс и навигация (`AutoTestsView.tsx`, `Sidebar.tsx`, `App.tsx`)**:
  - Создана новая вкладка «Автотесты & Runner» в боковом меню (секция «АРХИТЕКТУРА & КОД») с иконкой `FlaskConical`.
  - Полное соответствие дизайн-системе **Obsidian Metric** ([DESIGN.md](file:///d:/Вайбкодинг/Bitbucket/DESIGN.md)).
- **Тестирование и верификация**:
  - Добавлен тест 36 в `tests/engine.test.js` (`Autotests Module: TestExtractor parses test suites, calculates coverage, and generates test code`). Все 36 тестов пройдены успешно (100% pass).
  - Сборка `tsc --noEmit` и `npm run build` завершены без ошибок.

## [3.0.0] - 2026-09-01
### Внедрение минималистичной дизайн-системы Precision Dark Engineering Studio (Obsidian Metric) через Stitch
- **Дизайн-система & Спецификация (`DESIGN.md`)**:
  - Создана формализованная спецификация дизайн-системы в корне проекта `DESIGN.md` с опорой на принципы анти-слопа и тональной градации (Tonal Layering).
  - Сгенерирован проект в Google Stitch (`projects/12143355972581449827`) и эталонный экран `Engineering Dashboard - Overview`.
- **Глобальные токены и стили (`tailwind.config.js`, `index.css`)**:
  - Внедрена строгая палитра `Obsidian Metric`: Canvas `#090A0F`, Card Surface `#161922`, Panel Surface `#111318`, Hover `#1E222D`, Hairline Border `#1E2330`, Focus Border `#2E3748`, Brand Accent `#2563EB`.
  - Удален избыточный `glassmorphism` (`backdrop-filter: blur`, полупрозрачные фоны и неоновые тени).
  - Настроены тонкие графитовые скроллбары `#1E2330`.
- **Рефакторинг навигации и каркаса (`Sidebar.tsx`, `Header.tsx`, `App.tsx`)**:
  - Сайдбар: компактный фон `#0E1015`, тонкий 2px левый индикатор активного раздела (`border-l-2 border-blue-500 bg-blue-500/10 text-blue-300`).
  - Шапка: высота `48px` (`h-12`), монохромные плашки репозитория/ветки, чистая кнопка запуска анализа `#2563EB`.
- **Панель управления (`DashboardView.tsx`)**:
  - Устранен разнобой цветов («эффект конфетти»): монохромные карточки метрик с четкими крупными цифрами на `JetBrains Mono`.
  - Чистая группировка стека технологий и экранных форм с 1px разделителями.
- **Модальные окна и представления (`ScreenFormStructureModal.tsx`, `DataFlowsView.tsx`)**:
  - Модальные окна переведены на плоские графитовые подложки `#111318` с аккуратными радиусами `rounded-lg`.
  - Исправлена строгая TypeScript-типизация `NavTab` и `FlowStep` (100% pass `tsc --noEmit` и `npm run build`).
- **Тестирование и контроль версий**:
  - Проект зафиксирован в Git-репозитории `https://github.com/EvilOkta/Bitbucket-Analizer`: ветка `main` хранит исходное состояние, `feature/minimalist-redesign` — обновленный минималистичный дизайн.
  - Все 35 модульных тестов пройдены успешно (100% pass).

## [2.9.9] - 2026-09-01
### Расширенный парсинг экранных форм: правила интерактивности (IA-1, IA-2), позиционирования (POS-1, POS-2), атрибутов (ATTR-1) и анализ Redux/side-effects обработчиков
- **Детекция интерактивных элементов (`ПРАВИЛО IA-1 & IA-2`)**:
  - `flowTracer.ts`:
    - Внедрен справочник `INTERACTIVE_COMPONENTS` (Consta UI: `Button`, `Checkbox`, `Switch`, `RadioGroup`, `Select`, `Combobox`, `TextField`, `Textarea`, `Chip`, `Tabs`, `Collapse`, `SlideBar`, `SlideBlock`; Native HTML: `input`, `button`, `select`, `textarea`, `a`, `form`; React Hook Form: `Controller`, `ControllerField`; MUI/AntD/Element).
    - Внедрен набор интерактивных пропсов `INTERACTIVE_PROP_NAMES` (`onClick`, `onChange`, `onSubmit`, `onBlur`, `onFocus`, `onKeyDown`, `disabled`, `checked`, `selected`, `value`, `placeholder`, `type`, `icon`).
    - Реализована функция точной классификации элементов `classifyElementType` (`button.submit`, `button.reset`, `button.action`, `checkbox`, `radio`, `select`, `text_input`, `textarea`, `toggle`, `date_picker`, `file_input`, `modal`, `link`, `form`).
- **Позиционирование и структура контейнеров (`ПРАВИЛО POS-1 & POS-2`)**:
  - Алгоритм построения иерархического пути `JSXPath` (например: `/SlideBlock/div.root/form/div.footer/Button[1]`), расчет глубины вложенности `depth` и индекса соседей `siblingIndex`.
  - Извлечение значимых семантических контейнеров (`containers`) с фильтрацией пустых общих тегов.
  - Автоопределение UI-секции `detectUISection`: `modal`, `sidebar`, `header`, `footer`, `form`, `toolbar`, `table`, `card`.
- **Извлечение атрибутов и props (`ПРАВИЛО ATTR-1`)**:
  - Извлечение меток `label`, текстового содержимого, подсказок `placeholder`, иконок (`iconRight`, `iconLeft`, `<*Icon />`), пропсов состояния (`disabled`, `required`, `checked`, `readOnly`, `selected`, `type`, `value`).
- **Анализ обработчиков и сайд-эффектов (Redux Actions & Side Effects)**:
  - Парсинг функций-обработчиков в коде файла:
    - Детекция Redux-экшенов: `dispatch(actionName(...))` и `dispatch({ type: '...' })`.
    - Детекция побочных эффектов: `fetch(...)`, `axios.*`, `localStorage.*`, `sessionStorage.*`, `navigate(...)`.
    - Анализ ветвлений и условной логики (`if`, `switch`, `try/catch`).
  - Обогащение шагов Sequence-диаграммы новыми акторами (`Redux Store / State`, `Browser Storage`) и вызовами `dispatch(...)`/`localStorage.*`.
- **Интерфейс просмотра и инспектор структуры**:
  - `DataFlowsView.tsx`: в спойлере элемента отображаются JSX-путь, бейдж UI-секции, бейджи атрибутов/пропсов и вызываемые Redux-экшены с эффектами.
  - `ScreenFormStructureModal.tsx`: в инспекторе узла графа выводится карточка JSX-пути, секция UI, атрибуты/props и детальный блок Redux экшенов/эффектов.
- **Тестирование и сборка**:
  - Добавлен тест 35 в `tests/engine.test.js` (`UI Screen Form Analysis Rules`). Все 35 тестов пройдены успешно (100% pass).
  - Сборка `npm run build` завершена без ошибок.
  - Пересобрана portable Windows-версия `dist-package/Bitbucket Architecture Analyzer 1.0.0.exe`.

## [2.9.8] - 2026-09-01
### Очистка бокового меню: удаление счетчиков-бейджей у разделов навигации
- **UI / Боковое меню (`Sidebar.tsx`)**:
  - Удалены круговые бейджи со счетчиками количества сущностей (`stats.endpoints`, `stats.flows`, `stats.entities`, `stats.recs`) в правой части элементов навигации («Карта API», «Data Flows & Sequence», «Модель данных (ERD)», «Рекомендации»).
  - Упрощена структура пунктов меню и оптимизировано выравнивание кнопок (`justify-start`), что сделало навигационную панель более лаконичной и читаемой.
- **Тестирование и сборка**:
  - Успешная сборка renderer и electron (`npm run build`).
  - Все 34 модульных теста пройдены успешно (100% pass, `node --test tests/engine.test.js`).
  - Пересобрана портативная Windows-версия приложения `dist-package/Bitbucket Architecture Analyzer 1.0.0.exe` (98.1 MB).

## [2.9.7] - 2026-09-01
### Восстановление и модернизация D3 графа структуры проектов: поддержка монорепозиториев, подпроектов и связей эволюционных версий (BUG-058)
- **Интеграция монорепозиториев и версий в иерархию проектов (`BUG-058`)**:
  - `types.ts`: расширен интерфейс `HierarchyNode` (добавлен уровень узлов `type: 'subproject'`, поля `repoType`, `subprojects`, `similarityWith`, `isLocal`, `localPath`, `isGitInitialized`).
  - `bitbucketClient.ts`:
    - Реализован метод `buildHierarchyFromRepositories(repos: RepositoryItem[])`, строящий 4-уровневое связанное дерево проектов с учетом подпроектов Workspaces (`apps/web-client`, `services/order-api` и др.), веток, коммитов и метаданных сходства версий.
    - Обновлен `generateMockHierarchy` для покрытия всех сценариев корпоративных репозиториев (`ENTERPRISE` монорепо, `CORE` версионированные копии, `PROJ` микросервисы, `HIGH` HFT-сервис, `LOCAL` локальные папки).
  - `main.ts`:
    - В IPC-обработчике `get-project-graph` внедрена динамическая генерация иерархии из `localStore.getRepositories()`, что исключило зависание при блокирующих вызовах и синхронизировало граф с сохраненными и локальными репозиториями.
- **Интерактивная визуализация D3 Force Canvas (`ProjectGraphView.tsx`)**:
  - Добавлена специализированная цветовая дифференциация и иконки для узлов: `🏗️ Монорепозиторий` (purple), `🧬 Версия / Копия` (amber), `📦 Подпроект` (cyan), `💻 Локальный` (emerald), `📦 Микросервис` (sky/blue).
  - В режиме «2. Проект $\rightarrow$ Репозитории» внедрена отрисовка **эволюционных связей сходства** (пунктирные янтарные линии со значениями `88% сходства`).
  - В режиме «3. Репозиторий $\rightarrow$ Подпроекты / Ветки» добавлен фильтр по составу (`Все`, `Подпроекты`, `Ветки`) с раскрытием дочерних компонентов монорепозитория.
  - В режиме «4. Ветка / Подпроект $\rightarrow$ Коммиты» обеспечена каскадная синхронизация селекторов (Проект $\rightarrow$ Репозиторий $\rightarrow$ Ветка/Подпроект).
  - Реализована надежная локальная демо-заглушка `generateLocalFallbackHierarchy` и строгая валидация связей `validLinks` для 100% предотвращения ошибок `d3.forceLink`.
  - Обновлена боковая панель инспектора с карточками характеристик версий/монорепо и кнопкой прямого запуска анализа.
- **Тестирование и сборка**:
  - Добавлен тест 34 в `tests/engine.test.js` (`D3 Project Graph: Hierarchy Builder with Monorepos, Subprojects and Evolutionary Versions`). Все 34 теста пройдены успешно (100% pass).
  - Успешная сборка renderer и electron (`npm run build`).

## [2.9.6] - 2026-09-01
### Исправление разбора комментариев к свойствам объектов и PlantUML визуализация ER-диаграммы (BUG-056, BUG-057)
- **Точный построчный захват комментариев к свойствам объектов (`BUG-056`)**:
  - `postgresParser.ts`:
    - В `parseObjectLiteralProperties` добавлен поиск inline-комментария `// ...` на остатке текущей строки после завершающей запятой свойства (например, `log: [], // Array of ...`), что предотвратило потерю комментария у первого свойства и его ошибочную утечку к следующему свойству `errors`.
    - В `extractAttributesFromObjectLiteral` правило парсинга структуры объекта `structPattern` (`Array of { ... }`) поставлено с наивысшим приоритетом перед `unionPattern`, гарантируя создание подсущностей `LogItem` (с подтипом `LogTypeEnum`) и `ErrorItem` без ложной классификации всего массива как ENUM.
- **Поддержка PlantUML визуализации и экспорта ER-модели (`BUG-057`)**:
  - `types.ts`: добавлено поле `erDiagramPlantUml?: string` в интерфейс `DataModel`.
  - `postgresParser.ts`: реализован метод `generatePlantUmlErd` для генерации ERD в синтаксисе PlantUML (`entity ... { ... }`, `enum ... { ... }`, связи `||--o{`).
  - `MermaidViewer.tsx`: реализована двусторонняя конвертация Mermaid `erDiagram` $\leftrightarrow$ PlantUML ERD, обеспечена полная поддержка вкладок «PlantUML (Визуализация)» и «PlantUML (Код)» для модели данных.
  - `DataModelView.tsx`: проброшен `plantUmlCode` в `MermaidViewer`.
- **Тестирование и сборка**:
  - Добавлен тест 33 в `tests/engine.test.js` (`ERD PlantUML Conversion`). Все 33 теста пройдены успешно (100% pass).
  - Пересобрана портативная Windows-версия приложения `dist-package/Bitbucket Architecture Analyzer 1.0.0.exe` (98.1 MB).

## [2.9.5] - 2026-09-01
### Специализированное представление ENUM, парсинг union-комментариев и вложенных структур, обновленный интерфейс ER-модели (BUG-054, BUG-055)
- **Специализированное представление ENUM в табличном режиме (`BUG-054`)**:
  - `DataModelView.tsx`: для сущностей типа ENUM устранены псевдо-реляционные колонки (`VARCHAR`, `PK`, `Nullable`, `val_1`). Ключом перечисления является его имя, а детальный состав отображается в виде чистого списка допустимых значений с карточками/бейджами, таблицей спецификации литералов (#, Значение, Описание, Статус) и списком ссылающихся сущностей.
- **Интеллектуальное извлечение ENUM и структур из JS/TS (`BUG-055`)**:
  - `postgresParser.ts`:
    - Внедрен парсер `parseObjectLiteralProperties` со скобочно-сбалансированным разбором литералов, корректно считывающий многострочные массивы строк (например, `capabilities: [ '...', '...' ]` в `TOOLS_CONFIG`).
    - Распознавание литеральных объединений (union types) из комментариев и типов (`status: 'IDLE', // 'IDLE' | 'RUNNING' | 'PAUSED' | 'DONE' | 'STOPPED'`) с автогенерацией ENUM-сущности и связи.
    - Извлечение структур объектов из аннотаций комментариев (`log: [], // Array of { timestamp, type: 'info'|'success'|'error'|'warning', text }` $\rightarrow$ сущность `LogItem` + `LogTypeEnum`, `errors: [], // Array of { file, error }` $\rightarrow$ `ErrorItem`).
    - Парсинг TypeScript объединений `export type Name = 'A' | 'B' | 'C';` в ENUM сущности.
- **Обновленный интерфейс модели данных (Сворачиваемый Sidebar + Тоггл «Таблица» / «Визуализация»)**:
  - `DataModelView.tsx`:
    - Постоянный сворачиваемый левый фрейм (кнопка `PanelLeftClose` / `PanelLeftOpen`, компактный вид с мини-иконками) со списком таблиц/ENUM/системных типов.
    - Главный переключатель режима: **«Таблица»** (просмотр состава выбранной таблицы или чистый список значений ENUM) и **«Визуализация»** (D3 граф или Mermaid диаграмма).
    - Автоматическая плавная фокусировка и центрирование D3 графа на таблице, выбранной в левом фрейме.
- **Тестирование и сборка**:
  - Успешная сборка renderer и electron (`npm run build`).
  - Все 32 модульных теста пройдены успешно (100% pass, `node --test tests/engine.test.js`).
  - Пересобрана портативная Windows-версия приложения `dist-package/Bitbucket Architecture Analyzer 1.0.0.exe` (98.1 MB).

### Выделение ENUM сущностей, системных типов, бейджи источников классификации и переключатель представлений ERD (BUG-053)
- **Выделение ENUM перечислений в модели данных (`BUG-053`)**:
  - `types.ts`: добавлены свойства `isEnum?: boolean`, `enumValues?: string[]`, `isSystemType?: boolean`, `sourceType`, `sourceLabel` для `EntityModel` и `EntityAttribute`.
  - `postgresParser.ts`:
    - Реализовано автоопределение массивов строковых литералов (например, `capabilities: ['Автоматическая классификация...', 'Возможность исключать...', 'Безопасное выполнение...']`) с выделением их в отдельные сущности с типом `sourceType: 'enum'`, `sourceLabel: 'ENUM Перечисление'`, списком значений `enumValues` и связями `1:N` с родительской сущностью.
    - Добавлен парсинг объявлений `enum` в TypeScript файлах.
- **Включение системных типов в модель данных**:
  - `postgresParser.ts`: параметры и колонки, ссылающиеся на системные типы (`file`, `filenode`, `folder`, `foldernode`, `system`, `blob`, `stream`), автоматически регистрируются в модели данных как отдельные системные сущности (`sourceType: 'system_type'`, `sourceLabel: 'Системный тип'`) с FK связями.
- **Классификация источников сущностей**:
  - Каждая сущность классифицируется точным бейджем источника: `База данных (SQL DDL)`, `Prisma Schema`, `Python ORM`, `.NET EF Core`, `TypeScript Интерфейс`, `JavaScript Структура`, `API DTO`, `ENUM Перечисление`, `Системный тип`.
- **Сегментированный переключатель представлений (Тоггл View Modes)**:
  - `DataModelView.tsx`: добавлен 4-позиционный тоггл режимов отображения: `[ 📊 D3 Граф | 🧜‍♀️ Mermaid | 📋 Таблицы | 🔀 Раздельный ]`.
  - В режиме «Таблицы» инспектор разворачивается во весь доступный экран без вертикального вытеснения, с быстрыми фильтрами по типам (`Все`, `ENUM`, `Системные`), блоком значений перечисления и FK-навигацией.
- **Тестирование и сборка**:
  - Добавлены тесты 29 и 30 в `tests/engine.test.js` (`ERD ENUM Extraction & Classification`, `ERD System Types Inclusion`).
  - Все 30 тестов успешно пройдены (100% pass).
  - Пересобрана портативная версия приложения `Bitbucket Architecture Analyzer 1.0.0.exe`.

## [2.9.3] - 2026-08-31
### Восстановление скроллинга и отображения таблиц расшифровки ER-диаграммы (BUG-052)
- **Исправление скроллинга и доступности расшифровки (`BUG-052`)**:
  - `DataModelView.tsx`: добавлен класс `overflow-y-auto h-full max-w-6xl mx-auto` в корневой контейнер представления, обеспечивающий доступ к спискам таблиц и детальной таблице атрибутов сущности.
  - Добавлен переключатель «Репозиторий (N) / Демо (10)» в шапку страницы для удобного переключения между моделью проекта и демо-моделью.
  - Улучшена безопасность поиска по атрибутам `(e.attributes || []).some(...)`.
- **Тестирование и сборка**:
  - Модульные тесты: 28/28 тестов успешно пройдены (100% pass).
  - Пересобрана портативная версия приложения `Bitbucket Architecture Analyzer 1.0.0.exe`.

## [2.9.2] - 2026-08-31
### Исправление инициализации D3 графа и рендеринга вкладки ER-диаграммы (BUG-051)
- **Устранение ошибки открытия вкладки ERD (`BUG-051`)**:
  - `DataModelView.tsx`: корректно восстановлены реактивные состояния `diagramMode`, `viewMode`, расчет `nodesData` и `linksData` для D3 графа связей.
  - Добавлены защитные проверки на случай пустых или частично определенных сущностей (`selectedEntity?.attributes || []`, `ent.attributes?.length || 0`, опциональные цепочки в поиске связей).
- **Тестирование и сборка**:
  - Модульные тесты: 28/28 тестов успешно пройдены (100% pass).
  - Пересобрана портативная версия приложения `Bitbucket Architecture Analyzer 1.0.0.exe`.

## [2.9.1] - 2026-08-31
### Переход к исходному коду объявления сущностей и колонок ER диаграммы (BUG-050)
- **Фиксация путей и строк исходного кода (`BUG-050`)**:
  - `types.ts`: добавлены поля `sourceFile?: string` и `sourceLine?: number` в интерфейсы `EntityModel` и `EntityAttribute`.
  - `postgresParser.ts`: реализовано автоматическое вычисление и сохранение путей и номеров строк файлов во всех парсерах: SQL DDL, Prisma schema, Python ORM, .NET C# EF Core, TypeScript интерфейсов, JavaScript структур и API DTO моделей.
- **Интерактивная навигация в интерфейсе**:
  - `DataModelView.tsx`: добавлены кнопки быстрого перехода к исходному коду сущности в списке таблиц (иконка `ExternalLink` при наведении), в заголовке инспектора выбранной таблицы (бейдж `FileCode` с именем файла и строки), а также при клике на имя колонки в таблице атрибутов.
  - `App.tsx`: проброшен обработчик `handleNavigateToSource` для бесшовного открытия файла в Repo Explorer с подсветкой строки и плавающей кнопкой возврата «Назад к ER диаграмме».
- **Тестирование и сборка**:
  - Добавлен модульный тест `ERD Source Location` (`tests/engine.test.js`, 28 тестов, 100% pass).
  - Пересобрана портативная версия приложения `Bitbucket Architecture Analyzer 1.0.0.exe`.

## [2.9.0] - 2026-08-31
### Явные связи по типам сущностей (1:1 и 1:N) и вывод описаний атрибутов в ERD (BUG-049)
- **Связывание неявных FK по типам сущностей (`BUG-049`)**:
  - `postgresParser.ts`: реализована прямая резолюция типов моделей в `inferImplicitRelationships`.
  - Поля, типы которых совпадают с известными сущностями (`run: ANALYSISRUN`, `tree: FILENODE`, `endpoints: APIENDPOINT ARRAY`, `stack: STACKPROFILE ARRAY`), автоматически помечаются как `isForeignKey: true` со ссылкой на `${Target}.id` и явной генерацией связей (1:1 или 1:N).
  - На D3 Force Canvas и Mermaid ERD отрисовываются соединительные линии и бейджи `FK` со стрелками перехода.
- **Извлечение и отображение описаний атрибутов**:
  - `postgresParser.ts`: добавлен парсинг JSDoc, inline-комментариев (`// ...`, `/* ... */`), аннотаций и документации колонок в TypeScript интерфейсах и JavaScript структурах данных.
  - `DataModelView.tsx`: добавлена колонка «Описание» в таблицу атрибутов выбранной сущности и вывод комментариев атрибутов в Mermaid ERD диаграмме.
- **Тестирование и сборка**:
  - Добавлен модульный тест `ERD Type Matching` (`tests/engine.test.js`, 27 тестов, 100% pass).
  - Пересобрана портативная версия приложения `Bitbucket Architecture Analyzer 1.0.0.exe`.

## [2.8.9] - 2026-08-31
### Приоритетный вывод описания (title/label/комментарий) перед именем класса для элементов экранных форм (BUG-048)
- **Приоритетное наименование элементов UI (`BUG-048`)**:
  - `flowTracer.ts`: внедрен метод `extractElementMetadata`, извлекающий атрибуты описания (`title`, `aria-label`, `label`, `placeholder`, `description`), предшествующие комментарии (`// ...`, `/* ... */`) и имя класса (`className`, `class`).
  - Форматирование наименования элементов экранных форм по правилу: сначала описание, затем имя класса или тип компонента (например: `Уменьшить (Zoom Out) (p-1 hover:bg-gray-800...)`, `Экспорт отчета в CSV (btn-export)`).
  - Улучшено отображение во всех визуализациях: в списке элементов экранных форм, в Sequence-диаграммах Mermaid/PlantUML и на D3 графе структуры экранной формы.
- **Тестирование и сборка**:
  - Добавлен модульный тест `FlowTracer: extracts element description prior to class name` (`tests/engine.test.js`, 26 тестов, 100% pass).
  - Пересобрана портативная версия приложения `Bitbucket Architecture Analyzer 1.0.0.exe`.

## [2.8.8] - 2026-08-31
### Типизация элементов SET/ARRAY и отображение целевой таблицы для FK (BUG-047)
- **Детализация типов коллекций (`BUG-047`)**:
  - `postgresParser.ts`: реализована строгая типизация хранимых элементов для множеств (`SET`) и массивов (`ARRAY`):
    - `INTEGER ARRAY`, `VARCHAR ARRAY`, `BOOLEAN ARRAY`, `INTEGER SET`, `VARCHAR SET`;
    - Связанные с сущностями ERD типы: `FOLDER SET`, `CATEGORY SET`, `FILE ARRAY`, `FILEITEM ARRAY`, `USER ARRAY`;
    - Поддержка TypeScript сигнатур: `User[]` $\rightarrow$ `USER ARRAY`, `Set<Category>` $\rightarrow$ `CATEGORY SET`.
- **Отображение и навигация по целевым таблицам внешних ключей (FK Target)**:
  - `DataModelView.tsx`: в таблице атрибутов сущности:
    - В столбце «Тип данных» отображается стрелка и целевая таблица внешнего ключа: `UUID → users(id)` или `VARCHAR → FolderNode(id)`.
    - В столбце «Ключ» рядом с бейджем `FK` выводится кликабельный бейдж таблицы назначения.
    - Клик по имени целевой таблицы мгновенно переключает инспектор на выбранную связанную сущность.
- **Тестирование и сборка**:
  - Добавлен модульный тест `ERD Element Typification` (`tests/engine.test.js`, 25 тестов, 100% pass).
  - Пересобрана портативная версия приложения `Bitbucket Architecture Analyzer 1.0.0.exe`.

## [2.8.7] - 2026-08-31
### Извлечение ERD моделей данных из JavaScript/TypeScript структур, констант и маппингов (BUG-046)
- **Извлечение Frontend структур данных (`BUG-046`)**:
  - `postgresParser.ts`: реализован метод `parseJavaScriptObjectStructures`, анализирующий `.js`, `.jsx`, `.ts`, `.tsx`, `.vue`, `.svelte`, `.mjs` файлы на наличие:
    - Константных словарей и маппингов (например, `nodesMap = { 'Корень': { id, label, path, depth, categories: new Set(), files: [], ... } }`);
    - Экспортируемых интерфейсов и типов данных TypeScript (`export type / interface ...`);
    - Константных начальных состояний (`defaultUserState`, `initialFilterState`) и коллекций объектов (`mockFiles`, `mockCategories`).
  - Реализован автоматический вывод типов полей (`VARCHAR`, `INTEGER`, `NUMERIC`, `BOOLEAN`, `ARRAY`, `SET`, `JSONB`) и первичных ключей (`id`, `_id`, `key`).
  - **Интеллектуальное связывание сущностей**:
    - Связывание по суффиксу `*Id` / `*_id` (`folderId` $\rightarrow$ `FolderNode`, `roleId` $\rightarrow$ `Role`);
    - Связывание по коллекциям/массивам (`files: []` $\rightarrow$ `FileItem`, `categories: Set` $\rightarrow$ `Category`);
    - Связывание древовидных структур (`parentId` $\rightarrow$ `Node ||--o{ Node : "parent_of"`).
  - Безопасная генерация Mermaid ERD диаграммы и поддержка отрисовки на интерактивном D3 Force холсте в разделе «Модель данных (ERD)».
- **Тестирование и сборка**:
  - Добавлен модульный тест `Frontend JS/TS Data Model` (`tests/engine.test.js`, 24 теста, 100% pass).
  - Пересобрана портативная версия приложения `Bitbucket Architecture Analyzer 1.0.0.exe`.

## [2.8.6] - 2026-08-31
### 100% Автономный локальный рендеринг PlantUML диаграмм в темной теме (BUG-045)
- **Устранение зависимости от внешнего сервера plantuml.com (`BUG-045`)**:
  - `MermaidViewer.tsx`: реализован встроенный локальный (offline) SVG-компилятор PlantUML Sequence-диаграмм через `convertPlantUmlToMermaid` и клиентский векторный движок.
  - Полностью исключены сетевые запросы к `plantuml.com`, что устранило ошибки загрузки из-за блокировок провайдеров/РКН, корпоративных фаерволов и оффлайн-режима.
  - Визуализация PlantUML генерируется мгновенно (<10мс) с полной интерактивностью (Drag & Pan, Mouse Wheel Zoom 0.1x–5.0x, полноэкранный режим).
  - Вкладка «PlantUML Исходный код» по-прежнему предоставляет полноценный исходный код с полной палитрой темной темы skinparam для копирования в IntelliJ IDEA, VS Code или Confluence.
- **Тестирование и сборка**:
  - Добавлен модульный тест на двустороннюю конвертацию PlantUML $\rightarrow$ Mermaid (`tests/engine.test.js`, 23 теста, 100% pass).
  - Пересобрана портативная версия приложения `Bitbucket Architecture Analyzer 1.0.0.exe`.

## [2.8.5] - 2026-08-31
### Исправление визуализации PlantUML и внедрение темной темы оформления (BUG-044)
- **Исправление работы PlantUML визуализации (`BUG-044`)**:
  - `MermaidViewer.tsx`: устранено разрушение синтаксиса PlantUML (повторный запуск автопереноса `formatSequenceDiagram` дробил директивы `participant ... as ...` и приводил к сетевым/синтаксическим ошибкам).
  - Реализован безопасный транслятор из Mermaid в PlantUML (`convertMermaidToPlantUml`) при отсутствии прямого PlantUML-кода.
  - Добавлена асинхронная загрузка PlantUML SVG с векторной вставкой в интерактивный холст (полная поддержка зума и панорамирования аналогично Mermaid).
- **Стилизация под темную тему приложения (Dark Theme Skinparams)**:
  - Внедрена единая стилизация PlantUML:
    - Фоновый цвет: `#070A13` (бесшовно сливается с темным холстом приложения).
    - Акторы: изумрудная рамка `#10B981`, темный фон `#064E3B`, текст `#34D399`.
    - Участники/Компоненты: индиго `#6366F1`, фон `#1E1B4B`, текст `#A5B4FC`.
    - Базы данных: циан `#06B6D4`, фон `#083344`, текст `#67E8F9`.
    - Стрелки и вызовы: голубой `#38BDF8`, текст шагов `#93C5FD`.
    - Линии жизни и группы: `#475569`, `#1E293B`, `#0F172A`.
  - Обновлен метод `generatePlantUml` в `flowTracer.ts` и `DataFlowsView.tsx`.
- **Тестирование и Portable сборка**:
  - Добавлен модульный тест на генерацию и темную тему PlantUML (`tests/engine.test.js`, 22 теста, 100% pass).
  - Пересобрана портативная версия приложения `Bitbucket Architecture Analyzer 1.0.0.exe`.

## [2.8.4] - 2026-08-31
### Очистка кнопок в тулбаре элемента и динамическая легенда слоев D3 структуры (BUG-042, BUG-043)
- **Очистка тулбара выбранного элемента (`BUG-042`)**:
  - `DataFlowsView.tsx`: в шапке элемента над диаграммой удалены лишние дублирующие кнопки (`App (App.jsx)`, `Метод:...`, `Строка:...`, `DTO`).
  - Оставлены лаконичные текстовые бейджи названия формы и элемента, `Маршрут: ...` и единственная целевая кнопка **«Показать структуру»**.
- **Динамическая легенда слоев и переработка цветовой индикации (`BUG-043`)**:
  - `ScreenFormStructureModal.tsx`: реализован динамический расчет фактически присутствующих на схеме слоев (`activeLayerIds`).
  - Верхняя полоса заголовков слоев формируется строго из имеющихся на графе слоев с динамической нумерацией `1..N` (исключены фантомные DTO и База данных для frontend-проектов).
  - Термин «Backend Методы» заменен на нейтральный «Методы».
  - В нижнюю легенду категорий добавлено **«Событие (onLoad)»** (`bg-teal-500`), «Методы» (`bg-purple-500`), «Элементы UI» (`bg-blue-500`), «Форма» (`bg-emerald-500`).
  - Устранено безусловное добавление ноды `PostgreSQL DB` и ложных SQL-связей при анализе чистого фронтенда.
  - Позиционирование X-координат в D3 симуляции адаптируется под реальное количество активных колонок.
- **Тестирование и Portable сборка**:
  - Все 21 модульный тест пройдены успешно (`npm test`).
  - Пересобрана портативная версия приложения `Bitbucket Architecture Analyzer 1.0.0.exe`.

## [2.8.3] - 2026-08-31
### Отображение экранных форм на Dashboard, парсинг реальных методов и устранение заглушек (BUG-038 - BUG-041)
- **Панель управления / Dashboard (`BUG-038`)**:
  - `DashboardView.tsx`: добавлена карточка метрики «Экранных форм UI» (`screenForms.length` и суммарное число элементов) со ссылкой на `data-flows`.
  - Добавлен обзорный интерактивный блок «Экранные формы и элементы интерфейса» с карточками обнаруженных компонентов, маршрутов, тегами элементов и кнопкой перехода в Data Flows & Sequence.
- **Точный переход к коду объявления элементов и исключение .gitignore (`BUG-039`)**:
  - Вымышленные DTO заменены на прямую кликабельную ссылку на место в коде, где элемент объявлен (`file.jsx:line`).
  - `RepoExplorerView.tsx`: в `resolveTargetNode` устранен некорректный возврат первого файла репозитория (`allFiles[0]`, являвшегося `.gitignore`), если целевой файл не найден.
- **Парсинг реальных методов-обработчиков и фрагментов кода объявлений (`BUG-040`)**:
  - `FlowTracer.ts`: реализован построчный парсинг AST/JSX/HTML/Vue компонентов (`.jsx`, `.tsx`, `.vue`, `.html`, `.svelte`, `.xaml` и др.):
    - Определение точного номера строки объявления (`sourceLine`).
    - Извлечение реальных имен методов и функций (`onClick`, `onChange`, `onSubmit`, `useEffect`, `onMounted`) вместо вымышленных контроллеров.
    - Извлечение исходного фрагмента кода объявления элемента (`codeSnippet`).
    - Построение пошаговой Sequence-трассировки с реальными участниками (Пользователь $\rightarrow$ Компонент $\rightarrow$ Обработчик $\rightarrow$ API).
- **Удаление фейковых заглушек Frontend Payload (`BUG-041`)**:
  - `FlowTracer.ts`: удалена генерация статических мок-объектов JSON.
  - `DataFlowsView.tsx` & `ScreenFormStructureModal.tsx`: блок «Frontend Payload» отображается исключительно при наличии реальных параметров в коде и скрывается, если пэйлоад пустой.
- **Тестирование и сборка**:
  - Добавлены новые модульные тесты в `tests/engine.test.js` (всего 21 тест, 100% pass).
  - Сборка `npm run build` завершена без ошибок.

## [2.8.2] - 2026-08-28
### Исправление перехода к коду DTO/API и динамическое построение ER-диаграмм (BUG-036, BUG-037)
- **Приоритет исходного кода над `.md` файлами документации (`BUG-036`)**:
  - `RepoExplorerView.tsx`: введена пенализация не-кодовых файлов (`.md`, `.txt`, `.doc`, `.json`, `.yaml`) и бонус для файлов языков программирования (`.ts`, `.tsx`, `.cs`, `.py`, `.java`, `.go`, `.cpp`).
  - Поиск по содержимому ориентирован строго на синтаксические конструкции (`interface`, `class`, `type`, `struct`, `record`, `def`, функции), исключая совпадения по общему тексту в документации.
  - `ApiExtractor.ts`: добавлен метод `findImplementingSourceCode`, который связывает спецификации OpenAPI/Swagger с реальными файлами контроллеров в проекте.
  - `FlowTracer.ts`: поиск файлов DTO моделей переведен исключительно на исходный код.
- **Динамический парсер моделей данных и ER-диаграмм (`BUG-037`)**:
  - `PostgresParser.ts`: реализован мультиязычный парсер сущностей и схем:
    1. SQL DDL & Migrations (`CREATE TABLE`, `ALTER TABLE ... ADD FOREIGN KEY`, `REFERENCES`).
    2. Prisma schemas (`model Client { ... }`).
    3. Python SQLAlchemy & Django ORM (`class Model(Base): __tablename__ = ...`).
    4. .NET C# Entity Framework Core (`[Table] public class Entity { get; set; }`).
    5. TypeScript / TypeORM (`@Entity() class Entity`, `export interface Entity`).
    6. API DTOs Fallback (построение логической модели на основе Request/Response DTO).
  - Удалена статическая подстановка банковских таблиц (`roles`, `users`, `accounts`) при анализе реальных проектов.
  - `DataModelView.tsx`: исправлена реактивная синхронизация состояния `useSampleModel`, корректно переключающая холст на реальные сущности при завершении анализа.
- **Тестирование и сборка**:
  - Добавлены новые тесты в `tests/engine.test.js` (всего 19 тестов, 100% pass).
  - Успешно собрана обновленная версия Windows Portable: `dist-package/Bitbucket Architecture Analyzer 1.0.0.exe`.

## [2.8.1] - 2026-08-28
### Исправление ошибки открытия вкладки «Репозитории» (BUG-035)
- **UI / RepositoriesView.tsx**:
  - Добавлено корректное извлечение пропса `onOpenLocalFolder` в параметрах функции `RepositoriesView`.
  - Устранен `ReferenceError` в цикле рендеринга React, вызывавший белый экран при переходе на вкладку репозиториев.
  - Проверено отображение кнопки выбора локальной папки, бейджей классификатора (`💻 Локальный`, `⚠️ Без .git`, `🏗️ Монорепозиторий`, `🧬 Эволюционная копия`) и селектора веток.

## [2.8.0] - 2026-08-28
### Локальные репозитории, монорепозитории, классификатор копий и межрепозиторная трассировка
- **Локальный выбор репозиториев (Offline / Local Directory)**:
  - Интеграция с нативным проводником ОС (`dialog.showOpenDialog`) для выбора локальной папки.
  - Автоматическая проверка директории `.git`, чтение текущей ветки из `.git/HEAD` и предупреждающий бейдж `Без .git` при открытии простой папки с кодом.
  - Рекурсивное чтение файлов с фильтрацией мусорных каталогов (`node_modules`, `dist`, `bin`, `obj`, `__pycache__`, `.venv`, `.git`).
- **Поддержка монорепозиториев (Monorepo & Workspaces)**:
  - Создан модуль `MonorepoDetector` для распознавания Nx/Turborepo workspaces, .NET Solutions `.sln`/`*.csproj`, Gradle multi-project, Maven submodules и Python-сервисов.
  - В шапку приложения добавлен выпадающий селектор области анализа (выбор между анализом всего монорепозитория и изоляцией отдельного подпроекта).
- **Классификатор схожести и ролей репозиториев (`RepoClassifier`)**:
  - Алгоритм расчета Jaccard-схожести по структуре AST, путям файлов, эндпоинтам и моделям данных.
  - Автоматическое разделение репозиториев на роли: `📦 Микросервис`, `🧬 Эволюционная копия (N% сходства с v1)`, `🏗️ Монорепозиторий`, `📚 Shared Library`, `💻 Локальный`.
- **Сквозная межрепозиторная трассировка связей (`CrossServiceDependencies`)**:
  - Поиск исходящих REST-вызовов (`HttpClient`, `fetch`, `axios`, `httpx`) и брокеров сообщений (Kafka/RabbitMQ topics).
  - Построение направленных связей между независимыми сервисами и подпроектами.
- **Enterprise-набор согласованных тестовых проектов**:
  - `Enterprise-Monorepo` (React UI + .NET Orders API + FastAPI Payments API + Shared DTOs).
  - `BankingGateway-v1` и `BankingGateway-v2` (эволюционные копии с 88% схожестью).
  - `Orders-Service`, `Payment-Gateway`, `Notification-Service` (микросервисы с перекрестными вызовами и Kafka-топиками).
- **Тестирование**: Все 17 юнит-тестов в `tests/engine.test.js` пройдены успешно.

## [1.0.0] - 2026-08-25
### Реализация архитектурного анализатора и Windows Portable сборки
- **Frontend SPA (React + TypeScript + Vite + Tailwind CSS + Lucide)**:
  - Панель управления (Dashboard) со сводной статистикой и метриками.
  - Менеджер подключений (Connections) для Bitbucket Server PAT, Confluence Server PAT и локальной LLM Qwen с проверкой связи.
  - Селектор репозиториев (Repositories) с выбором веток и запуском анализа.
  - Интерактивное дерево проекта (Repository Explorer) с категоризацией слоев (Controllers, Services, Repositories, Models, Migrations, Tests, Configs).
  - Инспектор стека технологий (Stack Overview) с уровнем уверенности (Confidence) и доказательствами (Evidence).
  - Реестр API-методов (API Map) с фильтрацией по методам и экспортом в OpenAPI 3.0.
  - Интерактивный просмотр Sequence-диаграмм (Mermaid Viewer) и пошаговой трассировки Data Flow.
  - Логическая модель данных PostgreSQL (Data Model) с интерактивной ERD диаграммой и таблицей атрибутов.
  - Рекомендации по архитектуре и ИБ (Recommendations) на базе правил и обогащения нейросетью Qwen.
  - Мастер публикации отчетов в Confluence Server (Confluence Publisher).
  - Журнал аудита информационной безопасности (Audit Logs) с экспортом в JSON.
- **Analysis Engine**:
  - `StackDetector`: мультиязычный анализ Python, .NET (C#), JS/TS, C++, PostgreSQL, CI/CD.
  - `ApiExtractor`: парсинг FastAPI, Django, Flask, ASP.NET Core, Minimal APIs, Express, NestJS, Oat++, Crow.
  - `FlowTracer`: call-graph и генерация Mermaid Sequence Diagrams.
  - `PostgresParser`: парсинг SQL DDL и генерация Mermaid ERD.
  - `RuleEngine`: детекция антипаттернов (смешивание слоев, отсутствие тестов, захардкоженные секреты).
  - `QwenAdapter`: интеграция с локальной моделью Qwen по Bearer API токену и безопасным fallback.
- **Desktop Runtime (Electron + LocalStore)**:
  - Шифрование PAT-токенов алгоритмом AES-256 с машинным ключом (Windows DPAPI / Machine Key).
  - Изолированный IPC-мост (`contextIsolation: true`, `nodeIntegration: false`, typed preload).
  - Настройка сборки Windows Portable `.exe` в `electron-builder.yml`.
- **Информационная безопасность и Тестирование**:
  - Проведен статический аудит зависимостей (SAST / npm audit).
  - Модульные тесты `tests/engine.test.js` успешно пройдены (4/4 passed).
## [1.6.0] - 2026-08-25
### 4 режима D3-графа, группировка API по контроллерам, интерактивный ERD Canvas и экранные формы в Sequence
- **Оптимизация D3-графа структуры (4 сфокусированных представления)**:
  - Режим 1: `Bitbucket Server ➔ Ключи проектов`.
  - Режим 2: `Ключ проекта ➔ Репозитории` (с возможностью включить 3-й уровень веток).
  - Режим 3: `Репозиторий ➔ Ветки`.
  - Режим 4: `Ветка ➔ Коммиты` (с автором, хешем и сообщением).
  - Поддержка двойного клика / кнопок перехода (drill-down) между уровнями с сохранением цветовых градиентов активности (зеленый $\rightarrow$ красный).
- **Усовершенствование карты API**:
  - Методы сгруппированы по контроллерам в раскрывающемся дереве слева.
  - При выборе контроллера доступен сводный просмотр контроллера и таблицы всех его эндпоинтов.
  - Извлечение описаний контроллеров, методов и параметров из docstrings/комментариев в коде.
  - Отображение полного абсолютного пути эндпоинта (`Base route` + `Method route`).
- **Интерактивная модель данных (ERD Canvas)**:
  - Интерактивный D3/SVG холст с плавным зумированием (Zoom) и панорамированием (Pan) мышью.
  - Режим **«Окружение выбранной таблицы»**: показ только выбранной таблицы и таблиц, связанных с ней через Foreign Key.
  - Полноэкранный режим просмотра (Fullscreen).
- **Экранные формы и Sequence-диаграммы интерактивных действий**:
  - Автоматический реестр экранных форм (`.tsx`, `.vue`, `.html`, `.xaml` и др.).
  - Список всех интерактивных элементов на форме (кнопки, поля ввода, селекты).
  - Генерация сквозной Sequence-диаграммы при выборе элемента с отображением frontend-параметров (payload), DTO моделей и цепочки вызовов до PostgreSQL.
- **Сборка и Запуск**:
  - Приложение пересобрано и запущено: `dist-package/win-unpacked/Bitbucket Architecture Analyzer.exe`.

## [1.7.0] - 2026-08-27
### Swagger-подобный интерфейс API, парсер Swagger/OpenAPI, PlantUML & Fullscreen Sequence, исправление зависания ERD
- **Swagger / OpenAPI интерфейс и парсер спецификаций**:
  - Переработана страница карты API: верхний фрейм содержит фильтруемый список/таблицу методов с бейджами HTTP-методов, путями, именами обработчиков (`handler()`), контроллерами и описаниями.
  - Нижний фрейм обеспечивает мгновенную полную расшифровку выбранного метода на одном экране (без разделения на вкладки): параметры (Path, Query, Header), Request Body DTO модель и форматированный JSON пример, схема ответов со всеми статус-кодами (200, 201, 400, 401, 404, 500) и контекст исходного файла.
  - В `ApiExtractor` добавлен парсер файлов Swagger/OpenAPI 2.0 / 3.0 / 3.1 (`swagger.json`, `openapi.yaml` и др.) для автоматического извлечения точных полных путей, operationId, параметров и моделей.
- **Инструмент работы с Sequence диаграммами и экранными формами**:
  - Реализован глубокий парсинг всех UI файлов (`.tsx`, `.vue`, `.html`, `.xaml`, `.svelte`, `.razor` и др.) без искусственного лимита элементов.
  - Извлечение всех интерактивных элементов: кнопок (с иконками, текстом, `aria-label`, обработчиками событий), полей ввода, форм, селектов, ссылок меню и списков действий.
  - Добавлен поиск и фильтрация по формам и элементам интерфейса.
  - В `MermaidViewer` добавлен полноэкранный режим (Fullscreen) с модальным холстом, зумированием (Zoom In / Out / Reset), панорамированием и горячей клавишей Escape.
  - Реализован генератор синтаксиса **PlantUML** (`@startuml ... @enduml`) с переключателем вкладок и копированием кода.
- **Устранение критического зависания инструмента "ER диаграмма"**:
  - Устранена ошибка обращения к свойствам `rel.fromEntity` / `rel.toEntity` в `DataModelView.tsx` $\rightarrow$ приведены в соответствие с `sourceEntityName` / `targetEntityName`.
  - Добавлена строгая предварительная валидация связей перед симуляцией D3, исключающая передачу `undefined` нод в `d3.forceLink`.
  - Исправлено свойство `attr.isNullable`.
  - Добавлен переключатель между интерактивным D3 холстом и Mermaid ERD диаграммой.
## [1.8.0] - 2026-08-27
### Доработки API Map, PlantUML и Fullscreen в Sequence, стабильность ERD и адаптивная компактная верстка
- **API Map (Swagger-стиль & чистый сайдбар)**:
  - Левая панель контроллеров очищена от вложенных списков методов: теперь отображаются только имена контроллеров и компактные бейджи с общим количеством методов. Клик по контроллеру фильтрует верхний список.
  - Верхний фрейм: компактный перечень карточек эндпоинтов со Swagger-стилем (HTTP-метод, маршрут, имя функции `handler()`, имя контроллера, поиск и фильтры).
  - Нижний фрейм: полная расшифровка выбранного метода на едином экране **без вкладок** (параметры, тело запроса DTO + JSON, статус-коды ответов, исходный код).
- **Sequence диаграммы (PlantUML & Fullscreen)**:
  - Внедрена нативная генерация и подсветка синтаксиса **PlantUML** (`@startuml ... autonumber ... @enduml`) с возможностью мгновенного копирования для IntelliJ / VS Code / PlantUML Server.
  - Добавлен полноэкранный режим с зумированием, панорамированием, выходом по клавише ESC и экспортом в SVG.
  - Добавлен демонстрационный fallback для интерактивного тестирования экранных форм и UI-действий.
- **Интерактивная Модель данных (ERD)**:
  - Добавлены защитные механизмы против `undefined` сущностей и пустых моделей данных, исключающие белый экран и сбои React.
  - Встроен демонстрационный набор таблиц PostgreSQL (users, orders, order_items) для мгновенного визуального тестирования.
  - Доступны режимы «D3 Граф связей» и «Mermaid ERD» с режимом окружения выбранной таблицы.
- **Адаптивная верстка и предотвращение переполнения (Layout Safety)**:
  - Добавлены глобальные CSS-правила переноса строк (`overflow-wrap: anywhere`, `word-break: break-word`, `min-w-0`).
  - Уменьшены размеры шрифтов до компактных значений (`text-xs`, `text-[11px]`, `text-[10px]`).
  - Устранены выезды элементов за пределы экранов и контейнеров на всех страницах (Dashboard, Repositories, API Map, Sequence, Data Model).
- **Сборка и Тестирование**:
## [1.9.0] - 2026-08-27
### Сворачиваемый Sidebar и инлайн-аккордеоны (спойлеры) для методов API
- **Сворачиваемая главная левая панель (Sidebar)**:
  - Добавлена кнопка сворачивания в шапке панели (`PanelLeftClose` / `PanelLeftOpen`).
  - Компактный режим иконок (`w-16`) с сохранением состояния в `localStorage` и плавной CSS-анимацией.
  - Всплывающие подсказки (tooltips) и мини-индикаторы счетчиков при наведении курсора на иконки.
- **Инлайн-аккордеоны (спойлеры) методов на странице API Map**:
  - Карточки эндпоинтов преобразованы в инлайн-спойлеры (в стиле Swagger UI): при клике на метод детали (параметры, Request Body DTO + JSON, Response статус-коды, привязка к коду) открываются прямо внутри карточки.
  - Добавлены кнопки массового управления: **«Развернуть все»** и **«Свернуть все»** для быстрого просмотра всех эндпоинтов.
  - Левая панель контроллеров на странице API сделана сворачиваемой (`PanelLeftClose` / `PanelLeftOpen`) для освобождения 100% ширины экрана.
- **Сборка и Тестирование**:
  - Модульные тесты `npm test` выполнены успешно (7/7 passed).
  - Сборка приложения `npm run build` и Windows Portable исполняемого пакета `Bitbucket Architecture Analyzer.exe` завершена успешно.

## [2.0.0] - 2026-08-27
### Комплексное обновление Data Flows: двухуровневый аккордеон, PlantUML рендеринг, Portal Fullscreen, Pan/Zoom и экспорт в TXT
- **Единый левый фрейм экранных форм и элементов**:
  - Фреймы «Экранные формы» и «Элементы» объединены в единую компактную панель с двухуровневыми раскрывающимися аккордеонами (Форма $\rightarrow$ Интерактивные элементы).
  - Сайдбар форм снабжен кнопкой сворачивания (`PanelLeftClose` / `PanelLeftOpen`).
  - Фреймы параметров frontend (JSON payload) и DTO моделей аккуратно размещены под выбранным элементом.
- **Визуализация PlantUML**:
  - Реализован кодировщик PlantUML (UTF-8 Hex) и отрисовка полноценной визуальной диаграммы PlantUML наряду с интерактивным Mermaid.
  - Добавлены 4 вкладки: Mermaid Canvas, PlantUML Визуализация, PlantUML Код, Mermaid Код.
- **Полноэкранный режим (React Portal)**:
  - Fullscreen перенесен на `createPortal(..., document.body)` с z-index 9999, что гарантирует разворачивание на 100% монитора без ограничений родительских CSS-контейнеров.
- **Нативное панорамирование и зумирование диаграмм**:
  - Добавлена поддержка перемещения диаграммы мышью (Drag & Pan с курсорами `grab` / `grabbing`).
  - Добавлено масштабирование колесиком мыши (Mouse Wheel Zoom) в диапазоне от `0.1x` до `5.0x` и кнопка быстрого сброса `100%`.
- **Исправление работы страницы Data Flows (BUG-030)**:
  - Устранена ошибка `ReferenceError: useMemo is not defined` в `MermaidViewer.tsx` путем добавления явного импорта хука `useMemo` из `'react'`.
  - Добавлены защитные проверки на `undefined` для массивов `sequenceSteps`, `steps` и `frontendPayload` в `DataFlowsView.tsx`.

## [2.1.0] - 2026-08-27
### Доработки Data Flows: перемещение Payload/DTO в спойлеры элементов и надежный Fullscreen
- **Перемещение параметров Frontend Payload и DTO**:
  - Параметры Frontend (Payload JSON с кнопкой копирования) и целевая DTO модель вынесены напрямую внутрь аккордеона каждого интерактивного элемента в левой панели.
  - Центральный холст диаграммы теперь занимает 100% полезной высоты и ширины окна.
- **Исправление Fullscreen (Полноэкранный режим)**:
  - Изолированный fixed modal контейнер `z-[999999]` с `fixed inset-0 w-screen h-screen`, перекрывающий абсолютно все родительские слои и меню, с выходом по кнопке «Свернуть» и клавише `ESC`.
- **Сборка и верификация**:
## [2.4.0] - 2026-08-28
### Интерактивная навигация к коду API вызова, модальное окно DTO ответа (Scalar & Array) и исправление отображения Request Body
- **Кликабельный фрейм «Исходный код» в Карте API**:
  - Карточка «Исходный код метода» сделана интерактивной: клик осуществляет бесшовный переход во вкладку «Дерево проекта» (`RepoExplorerView`).
  - Автоматическое раскрытие всех родительских каталогов до целевого файла.
  - Подсветка строки кода метода с ярким индикатором и плавная прокрутка к строке (`scrollIntoView`).
  - На вкладке «Дерево проекта» при таком переходе появляется заметная кнопка **«← Назад к API»**, возвращающая пользователя обратно на Карту API.
- **Интерактивные DTO ответов вместо статического «JSON payload»**:
  - Вместо статической надписи выводится реальное наименование DTO модели / типа ответа (`OrderResponseDTO`, `UserDto[]`, `int`, `int[]`, `string[]` и др.).
  - Клик по бейджу открывает полнофункциональное модальное окно **ResponseDtoModal**:
    - Заголовок с именем DTO, статус-кодом (200 OK) и категорией (Скалярное значение, Массив скаляров, Объектная модель, Массив объектов).
    - Таблица полей: Параметр / Поле, Тип данных, Описание, Обязательность, Пример.
    - Корректная обработка единичных скалярных значений (`int`, `string`, `bool`) и массивов (`int[]`, `UserDto[]`).
    - Форматированный пример JSON с возможностью копирования в один клик.
- **Исправление парсинга и отображения структуры Request Body (BUG-031)**:
  - В `ApiExtractor` реализован глубокий резолвинг схем `$ref` для OpenAPI 3.x (`components.schemas`) и Swagger 2.0 (`definitions` и параметры `in: "body"`).
  - Генерация структурированного объекта `requestBody` со списком свойств `properties`, типами, описаниями и JSON-примерами для Swagger/OpenAPI и AST контроллеров (.NET, Python FastAPI, TypeScript, C++).
  - В Карте API добавлена таблица полей тела запроса с типами и признаком `REQUIRED` наряду с JSON-примером.
- **Тестирование и Сборка**:
  - Добавлены юнит-тесты на парсер схем `$ref`, Request Body, Response DTO и навигацию по дереву файлов. Все 10/10 тестов успешно пройдены (`node --test`).
  - Сборка Vite и Electron (`npm run build`) завершена без ошибок.
  - Сгенерирован автономный исполняемый Windows Portable файл: `dist-package/Bitbucket Architecture Analyzer 1.0.0.exe` (98.1 МБ).

## [2.5.0] - 2026-08-28
### D3 граф структуры экранной формы, событие инициализации формы (screen_load), навигация в дерево проекта и оптимизация панели кнопок
- **Кнопка «Показать структуру» (D3 граф структуры экранной формы)**:
  - Создан компонент `ScreenFormStructureModal.tsx` с интерактивным D3 force-directed графом.
  - Визуализирует полную иерархию связей экранной формы:
    - Корневая форма (зеленый узел)
    - События загрузки/инициализации (`screen_load`, бирюзовый узел)
    - Интерактивные элементы формы (кнопки, инпуты, селекты, синие узлы)
    - Обработчики бэкенда (фиолетовые узлы)
    - DTO модели запросов и ответов (индиго узлы)
    - Физическая база данных PostgreSQL (голубой узел)
  - Интерактивный инспектор узлов справа с детальным описанием, отображением JSON payload и прямой кнопкой перехода к файлу в дереве проекта.
  - Плавное панорамирование, перетаскивание узлов с физической симуляцией и управление масштабом (Zoom In/Out/Reset).
- **Экранная форма как элемент взаимодействия (`screen_load` / lifecycle)**:
  - В `flowTracer.ts` реализована генерация стартового элемента жизненного цикла экранной формы (`screen_load` / `lifecycle`).
  - Моделирует вызовы `useEffect` / `componentDidMount`, предзагрузку справочников и кэширование данных с бэкенда и БД PostgreSQL.
  - В списке элементов формы снабжен ярким бейджем `ON LOAD` и иконкой искры.
- **Оптимизация панели визуализации (`MermaidViewer.tsx`)**:
  - Панель инструментов переведена на компактный однострочный flex-layout без переноса строк.
  - Элементы зума сгруппированы в аккуратный единый блок (`ZoomOut`, `Zoom%`, `ZoomIn`, `Reset`, `SVG`).
  - Компактные надписи вкладок Mermaid/PlantUML.
- **Кликабельные методы, DTO и пути с переходом в «Дерево проекта»**:
  - Названия экранных форм, методов обработчиков (`targetAction`), DTO моделей (`dtoModel`) и шагов трассировки снабжены ссылками и иконками `↗`.
  - Клик мгновенно переключает вкладку на «Дерево проекта» (`RepoExplorerView`) с автоматическим раскрытием папок, выбором целевого файла и подсветкой строки кода.
## [2.6.0] - 2026-08-28
### D3 граф Left-to-Right, динамическая кнопка «Назад к экранным формам», прямые ссылки DTO на модели и выпадающий список диаграмм
- **Динамическая кнопка «Назад к экранным формам» / «Назад к API»**:
  - В `App.tsx` и `RepoExplorerView.tsx` реализовано сохранение исходной вкладки (`returnTab: 'data-flows' | 'api-map'`).
  - При переходе из экранных форм в Дереве проекта отображается кнопка **«← Назад к экранным формам»**, возвращающая пользователя в раздел форм.
  - При переходе из Карты API отображается **«← Назад к API»**.
- **Ориентация D3 графа структуры слева направо (Left-to-Right)**:
  - В `ScreenFormStructureModal.tsx` построена строгая послойная структура ($X$-layers):
    - Слой 1 (слева): Экранная форма (`category: 'form'`)
    - Слой 2: Интерактивные элементы формы и `screen_load` (`category: 'button' | 'input' | 'screen_load'`)
    - Слой 3: Обработчики и методы бэкенда (`category: 'action'`)
    - Слой 4: DTO модели данных (`category: 'dto'`)
    - Слой 5 (справа): База данных PostgreSQL (`category: 'database'`)
  - Связи визуализированы плавными горизонтальными кривыми Безье (Cubic Bezier) с направленными стрелками.
  - Вверху холста добавлена статусная полоса названий слоев.
- **Прямая привязка DTO к файлам моделей**:
  - В `flowTracer.ts` и `DataFlowsView.tsx` клики по DTO моделям открывают непосредственно файл модели (`src/models/${dto}.ts`, `src/dtos/${dto}.cs`), а клики по обработчику — файл контроллера.
- **Выпадающий список режимов диаграмм в `MermaidViewer.tsx`**:
  - Горизонтальный ряд кнопок табов заменен компактным кастомным выпадающим списком (Dropdown / `<select>`), позволяющим выбирать `📊 Mermaid Canvas`, `🖼️ PlantUML Визуализация`, `📄 PlantUML Исходный код`, `📄 Mermaid Исходный код`.
- **Тестирование и Сборка**:
  - Добавлен юнит-тест на послойный порядок графа и резолвинг путей моделей DTO (все 13/13 тестов пройдены успешно).
  - Сборка Vite и Electron (`npm run build`) завершена без ошибок.
  - Пересобрана автономная Windows Portable версия: `dist-package/Bitbucket Architecture Analyzer 1.0.0.exe` (98.1 МБ).

## [2.7.0] - 2026-08-28
### Полная синхронизация кодовой базы (Экранные формы, DTO, Контроллеры, Сервисы, БД) и двухстрочная панель визуализатора
- **Полная синхронизация исходных файлов в тестовых репозиториях**:
  - В `main.ts` (`generateEnterpriseSampleFiles`) внедрена генерация синхронизированных исходных файлов:
    - Экранные формы: `src/client/views/TransferMoneyForm.tsx`, `src/client/views/AccountHistoryView.vue`, `src/client/components/OtpConfirmModal.tsx`, `src/components/LoginForm.tsx`, `src/components/CheckoutForm.tsx`.
    - DTO модели: `src/models/TransferMoneyDTO.ts`, `src/models/AccountHistoryDTO.ts`, `src/models/OtpConfirmDTO.ts`, `src/models/LoginDto.ts`, `src/models/OrderDto.ts`.
    - Контроллеры: `src/controllers/PaymentController.ts`, `src/controllers/CatalogController.ts`, `src/controllers/AuthController.ts`, `src/controllers/OrderController.ts`, `src/Controllers/AccountsController.cs`.
    - Сервисы и БД: `src/services/PaymentService.ts`, `database/schema.sql`, `migrations/versions/001_initial_schema.sql`.
  - Устранен баг с открытием пустого дерева проекта: все ссылки из экранных форм, DTO бейджей, карточек методов и шагов трассировки открывают реальный файл в Дереве проекта с подсветкой строки кода.
- **Двухстрочная панель управления в визуализаторе (`MermaidViewer.tsx`)**:
  - Панель разделена на две строки:
    - **Строка 1**: Заголовок диаграммы (слева) + Выпадающий список выбора формата диаграммы (по центру) + Кнопка `Fullscreen` (справа).
    - **Строка 2**: Группа кнопок зума (`-`, `%`, `+`, `100%`, `SVG`) слева + Кнопка копирования кода (`Copy Mermaid / Copy PlantUML`) с подсказкой панорамирования справа.
  - Все кнопки полностью видны без перекрытия и наложения на любых разрешениях экрана.
- **Тестирование и Сборка**:
  - Все 13 юнит-тестов в `tests/engine.test.js` пройдены успешно.
  - Сборка Vite и Electron (`npm run build`) завершена без ошибок.
  - Пересобрана автономная Windows Portable версия: `dist-package/Bitbucket Architecture Analyzer 1.0.0.exe` (98.1 МБ).












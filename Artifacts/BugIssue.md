# BugIssue (Реестр ошибок и дефектов)

В данном файле регистрируются выявленные ошибки, баги и проблемы системы, а также статус их исправления.

## Шаблон для добавления новой записи

```markdown
### [BUG-XXX] Краткое описание проблемы
- **Дата обнаружения**: ГГГГ-ММ-ДД
- **Компонент**: (Backend / Frontend / Parser / Bitbucket Connector / Confluence / LLM / DB)
- **Критичность**: (Критическая / Высокая / Средняя / Низкая)
- **Статус**: (Новый / В работе / Исправлен / Закрыт)
- **Шаги воспроизведения**:
  1. Шаг 1
  2. Шаг 2
- **Ожидаемое поведение**: Описание
- **Фактическое поведение**: Описание
- **Причина и решение**: Описание исправления (при закрытии)
```

---

## Таблица зарегистрированных дефектов

| ID | Дата | Заголовок | Компонент | Критичность | Статус | Решение / Комментарий |
|:---|:-----|:----------|:----------|:------------|:-------|:----------------------|
| BUG-001 | 2026-08-25 | Уязвимости в транзитивных dev-зависимостях сборщика | Packaging / Dependencies | Высокая | Исправлен | Выполнен `npm audit fix`, зависимости обновлены до безопасных версий. |
| BUG-002 | 2026-08-25 | Темно-синий экран при запуске (не загружается UI) | Desktop Shell / Main Process | Высокая | Исправлен | Исправлено относительное разрешение пути к `dist/index.html` и `preload.js` в `main.ts` с динамическим fallback через `app.getAppPath()`. |
| BUG-003 | 2026-08-25 | Кнопки «Анализировать» выходят за границы карточек | UI / Repositories View | Средняя | Исправлен | Переработана flex-верстка карточек и адаптивный футер с подсказкой назначения анализа. |
| BUG-004 | 2026-08-25 | Недостаточно детальные входные/выходные параметры в карте API | UI & Engine / API Map | Средняя | Исправлен | Добавлены вкладки параметров: Path/Query params, Request Body DTO с примером JSON, Response Schema и статус-коды. |
| BUG-005 | 2026-08-25 | Ошибка парсинга Mermaid ERD (сплит по запятым в типах `NUMERIC(10,2)`) | Engine / PostgresParser | Высокая | Исправлен | Реализован парсер DDL с учетом вложенных скобок и валидацией строгих типов для Mermaid ERD. |
| BUG-006 | 2026-08-25 | При анализе репозитория не скачиваются реальные файлы из Bitbucket | Integrations / BitbucketClient | Высокая | Исправлен | Реализован `fetchRepositoryFiles` через Bitbucket Server REST API (`/files` и `/raw`), скачивающий реальные исходники репозитория под PAT. |
| BUG-007 | 2026-08-25 | В связях ERD выводится статический текст "references" вместо имени FK | Engine / PostgresParser | Средняя | Исправлен | На ребрах связей Mermaid ERD подставляется реальное имя поля внешнего ключа (например, `user_id`, `account_id`). |
| BUG-008 | 2026-08-25 | Список пространств Confluence не подтягивает динамические пространства под PAT | Integrations / ConfluenceClient | Средняя | Исправлен | Убраны хардкод-значения, настроена динамическая загрузка реальных пространств через `/rest/api/space` с сортировкой. |
| BUG-009 | 2026-08-25 | Отсутствие интерактивных тестов связи для Qwen LLM, Confluence и PG | UI & Integrations / Connections | Средняя | Исправлен | Добавлен интерактивный виджет пробных запросов к Qwen LLM с замером задержки (мс), отображением количества пространств Confluence и диагностикой PostgreSQL. |
| BUG-010 | 2026-08-25 | Результаты анализа выбранного репозитория не переключали экран и вызывались со старым стейтом | UI State / App.tsx | Высокая | Исправлен | Целевой репозиторий и ветка передаются напрямую в `onRunAnalysis`, по завершении анализа интерфейс автоматически переключается на Dashboard с обновлением всех счетчиков и графиков. |
| BUG-011 | 2026-08-25 | Отсутствие двухуровневой группировки репозиториев по ключу проекта | UI / Repositories View | Средняя | Исправлен | Реализована иерархическая группировка по `projectKey` с раскрывающимися блоками проектов, счетчиками и фильтрацией. |
| BUG-012 | 2026-08-25 | Подключение к LLM требует модель вместо универсального LiteLLM прокси | Engine & UI / QwenAdapter | Высокая | Исправлен | Настроена интеграция с LiteLLM прокси (Server URL + API Key без обязательного имени модели) и авто-роутингом. |
| BUG-013 | 2026-08-25 | В карте API отсутствует JSON представление для Request параметров и тела | UI / ApiMapView | Средняя | Исправлен | Добавлен переключатель формата «Таблица параметров / JSON представление» с валидной структурой и кнопкой копирования. |
| BUG-014 | 2026-08-25 | В Confluence выводятся пространства без прав на запись (read-only) | Integrations / ConfluenceClient | Средняя | Исправлен | Реализована фильтрация пространств с проверкой прав создания/редактирования страниц через `/rest/api/space?expand=permissions`. |
| BUG-015 | 2026-08-25 | Проекты в списке репозиториев по умолчанию открыты вместо свернутого состояния | UI / Repositories View | Низкая | Исправлен | Инициализация всех групп проектов как свернутых спойлеров по умолчанию с кнопками «Развернуть все» и «Свернуть все». |
| BUG-016 | 2026-08-25 | В репозиториях выводились статические ветки, из-за чего анализ падал при ветке отличной от `main` | Engine & UI / Bitbucket | Высокая | Исправлен | Реализована динамическая подгрузка реальных веток из Bitbucket Server per-repo и автоматический fallback на HEAD ветку в `fetchRepositoryFiles`. |
| BUG-017 | 2026-08-25 | Пагинация пространств Confluence и отсутствие ручного ввода ключа пространства | Integrations & UI / Confluence | Средняя | Исправлен | Увеличен лимит выборки пространств (global + personal), добавлена возможность прямого ручного ввода точного ключа пространства (Space Key). |
| BUG-018 | 2026-08-25 | В Confluence выводятся личные пространства пользователей | Integrations / ConfluenceClient | Низкая | Исправлен | Личные пространства (`type=personal` и `~username`) строго исключены, загружаются только публичные глобальные пространства. |
| BUG-019 | 2026-08-25 | Ограничение анализа репозитория лимитом в 200 файлов | Engine / BitbucketClient | Средняя | Исправлен | Искусственный лимит в 200 файлов удален, внедрена высокопроизводительная параллельная загрузка всех исходников репозитория без остановок. |
| BUG-020 | 2026-08-25 | Перегрузка D3-графа структуры при большом количестве нод | UI / ProjectGraphView | Средняя | Исправлен | Реализованы 4 изолированных представления (Bitbucket-Проекты, Проект-Репозитории, Репозиторий-Ветки, Ветка-Коммиты) с drill-down навигацией. |
| BUG-021 | 2026-08-25 | Методы API не были сгруппированы по контроллерам и не отображали полные пути | Engine & UI / ApiMapView | Средняя | Исправлен | Методы сгруппированы по контроллерам в иерархическом дереве, отображается полный маршрут и описания из docstrings. |
| BUG-022 | 2026-08-25 | ERD не поддерживала зум/панорамирование мышью, режим окружения и fullscreen | UI / DataModelView | Средняя | Исправлен | Разработан интерактивный холст ERD с зумом/панорамированием, режимом показа окружения выбранной таблицы и полноэкранным режимом. |
| BUG-023 | 2026-08-25 | В Sequence диаграммах отсутствовала привязка к экранным формам и UI-элементам | Engine & UI / DataFlowsView | Средняя | Исправлен | Добавлен реестр экранных форм и интерактивных элементов с генерацией Sequence диаграмм, frontend-параметров и DTO. |
| BUG-024 | 2026-08-27 | Зависание приложения при открытии инструмента "ER диаграмма" | UI & D3 / DataModelView | Критическая | Исправлен | Исправлены свойства связей `fromEntity`/`toEntity` на `sourceEntityName`/`targetEntityName`, добавлена строгая фильтрация несуществующих узлов для `d3.forceLink`, `isNullable` и переключатель на Mermaid ERD. |
| BUG-025 | 2026-08-27 | Неполный перечень экранных форм и UI-элементов в Sequence диаграммах | Engine / FlowTracer | Высокая | Исправлен | Расширен охват компонентов (React, Vue, Angular, Svelte, XAML, HTML), снято ограничение `slice(0, 8)`, добавлены кнопки с иконками/вложенным текстом, формы, селекты, инпуты и ссылки. |
| BUG-026 | 2026-08-27 | Sequence диаграмма не разворачивалась на весь экран и отсутствовал PlantUML | UI / MermaidViewer & DataFlowsView | Средняя | Исправлен | Добавлен полноэкранный режим с модальным холстом, зумированием, панорамированием, экспортом SVG и переключателем Mermaid/PlantUML. |
| BUG-027 | 2026-08-27 | Некорректный путь эндпоинтов, отсутствие имен методов и разделение на вкладки в API Map | Engine & UI / ApiMapView & ApiExtractor | Средняя | Исправлен | Реализован Swagger/OpenAPI парсер спецификаций, интерфейс переработан в Swagger-стиль с верхним фреймом списка/таблицы методов и нижним фреймом полной расшифровки без вкладок. |
| BUG-028 | 2026-08-27 | Список контроллеров слева содержал методы, а расшифровка параметров была разбита вкладками | UI / ApiMapView | Средняя | Исправлен | Сайдбар контроллеров слева очищен от вложенных списков методов (оставлены только бейджи с числом методов), расшифровка параметров перенесена в единый нижний фрейм без вкладок. |
| BUG-029 | 2026-08-27 | Выход элементов за границы экрана (поплывшая верстка), отсутствие PlantUML/Fullscreen и не открывающийся ERD | UI / Layout & Views | Высокая | Исправлен | Добавлены глобальные CSS правила переноса (`break-words`, `overflow-wrap: anywhere`, `min-w-0`), уменьшен шрифт, встроен sample fallback и безопасные D3 guards для ERD, внедрены PlantUML и Fullscreen в Sequence-вью. |
| BUG-030 | 2026-08-27 | Страница Data Flows не открывалась из-за отсутствующего импорта useMemo в MermaidViewer | UI / MermaidViewer & DataFlowsView | Критическая | Исправлен | Добавлен импорт `useMemo` из `react` в `MermaidViewer.tsx`, добавлены защитные проверки на `undefined` для массивов `sequenceSteps`, `steps` и `frontendPayload` в `DataFlowsView.tsx`. |
| BUG-031 | 2026-08-28 | Структура Request не отображалась в карте API даже при наличии в Swagger, некликабельный DTO ответа и код | Engine & UI / ApiExtractor & ApiMapView | Высокая | Исправлен | Реализован глубокий резолвинг `$ref` схем и `in: "body"` в `ApiExtractor`, генерация `requestBody` полей и примеров. Внедрена интерактивная модалка DTO ответа с поддержкой scalar (`int`), array (`int[]`, `UserDto[]`) и объектов. Добавлен переход по клику на исходный код с подсветкой строки в дереве проекта и кнопкой «Назад к API». |
| BUG-032 | 2026-08-28 | Открытие пустого дерева проекта при клике на DTO и экранные формы из-за отсутствия файлов в тестовом репозитории | Engine & Mock Repos / main.ts & flowTracer.ts | Высокая | Исправлен | В тестовые репозитории добавлены реальные синхронизированные файлы экранных форм (`TransferMoneyForm.tsx`, `AccountHistoryView.vue`, `OtpConfirmModal.tsx`, `LoginForm.tsx`, `CheckoutForm.tsx`), DTO моделей (`TransferMoneyDTO.ts`, `AccountHistoryDTO.ts`, `OtpConfirmDTO.ts`, `LoginDto.ts`, `OrderDto.ts`), контроллеров и БД, обеспечена прямая привязка путей при парсинге. |
| BUG-033 | 2026-08-28 | Перекрытие и скрытие кнопок зума, сброса и копирования в тулбаре MermaidViewer из-за выпадающего списка | UI / MermaidViewer.tsx | Средняя | Исправлен | Тулбар визуализатора перекомпонован в 2 строки: строка 1 — заголовок, селект и Fullscreen; строка 2 — группа зум-контролов (-, %, +, 100%, SVG) и кнопка копирования кода. |
| BUG-034 | 2026-08-28 | Зависание и белый экран при переходе в дерево проекта из-за отсутствия ссылки на codeLineRef | UI / RepoExplorerView.tsx | Критическая | Исправлен | Добавлено явное объявление рефа `codeLineRef` (`useRef<HTMLTableRowElement | null>(null)`) и состояния `copiedCode`, вызов `scrollIntoView` обернут в безопасный асинхронный таймаут с обработкой ошибок. |
| BUG-035 | 2026-08-28 | Вкладка «Репозитории» не открывалась из-за пропущенного пропса `onOpenLocalFolder` в параметрах компонента | UI / RepositoriesView.tsx | Высокая | Исправлен | В список деструктурируемых пропсов функции `RepositoriesView` добавлен пропущенный `onOpenLocalFolder`, устранен `ReferenceError` при рендеринге тулбара. |
| BUG-036 | 2026-08-28 | Ссылки на DTO/API в локальном репозитории вели на .md файлы документации вместо кода методов | Engine & UI / RepoExplorerView & ApiExtractor & FlowTracer | Высокая | Исправлен | Введена строгая пенализация (.md/.txt) и поиск деклараций `interface`, `class`, `type`, `struct`, `def` исключительно в файлах исходного кода. При OpenAPI-парсинге эндпоинты связываются с реальными контроллерами в коде. |
| BUG-037 | 2026-08-28 | ER диаграмма при анализе репозитория не изменялась и показывала тестовые банковские таблицы | Engine & UI / PostgresParser & DataModelView | Высокая | Исправлен | Реализован глубокий парсинг SQL DDL, Prisma schemas, моделей SQLAlchemy, EF Core, TypeORM и API DTOs. Удалена статическая подстановка мок-таблиц при анализе реальных репозиториев, настроена синхронизация стейта в `DataModelView`. |
| BUG-038 | 2026-08-31 | На панели управления (Dashboard) не отображаются результаты анализа экранных форм | UI / DashboardView.tsx | Средняя | Исправлен | Добавлена карточка метрики «Экранных форм UI» в общую сетку и отдельный обзорный блок с перечнем UI-форм, маршрутов, элементов и кнопкой прямого перехода в Data Flows. |
| BUG-039 | 2026-08-31 | Клик по DTO элемента экранной формы открывает .gitignore из-за отсутствия файла и fallback | UI & Engine / RepoExplorerView & FlowTracer | Высокая | Исправлен | Вымышленные DTO заменены на кликабельную ссылку на объявление элемента в коде (`file.jsx:line`), убран некорректный fallback на `allFiles[0]` (.gitignore) в `RepoExplorerView`. |
| BUG-040 | 2026-08-31 | Имя метода и DTO в экранных формах выдумывались парсером вместо извлечения реальных функций из JSX/HTML | Engine / FlowTracer.ts | Высокая | Исправлен | Реализован построчный парсинг AST/JSX/HTML в `FlowTracer`: извлечение реальных функций обработчиков (`onClick`, `onChange`, `onSubmit`, `useEffect`), точных строк и фрагментов кода объявлений. |
| BUG-041 | 2026-08-31 | В блоке Frontend Payload отображается фейковая заглушка даже при отсутствии параметров | Engine & UI / FlowTracer & DataFlowsView | Средняя | Исправлен | Удалена генерация фиктивных JSON заглушек в `FlowTracer`, блок «Frontend Payload» полностью скрывается в `DataFlowsView` и модальном окне при отсутствии реального пэйлоада. |
| BUG-042 | 2026-08-31 | Лишние кнопки в верхней шапке элемента экранной формы над диаграммой | UI / DataFlowsView.tsx | Низкая | Исправлен | Удалены дублирующие кнопки навигации из верхней панели над диаграммой, оставлены лаконичные текстовые бейджи и единственная кнопка «Показать структуру». |
| BUG-043 | 2026-08-31 | В легенде D3 структуры формы присутствуют отсутствующие на схеме элементы, отсутствует «Событие», «Backend методы» не переименованы | UI / ScreenFormStructureModal.tsx | Средняя | Исправлен | Реализован динамический расчет активных слоев графа, верхняя полоса заголовков отображает строго имеющиеся слои с перенумерацией 1..N, «Backend методы» переименованы в «Методы», в нижнюю легенду добавлено «Событие (onLoad)», убран безусловный мок БД. |
| BUG-044 | 2026-08-31 | Ошибка визуализации PlantUML и светлая тема диаграммы | Engine & UI / MermaidViewer.tsx, FlowTracer.ts, DataFlowsView.tsx | Высокая | Исправлен | Устранено искажение синтаксиса PlantUML повторным переносом строк, внедрена полноценная темная тема skinparam (`#070A13`, `#0F172A`, `#10B981`, `#06B6D4`, `#6366F1`) и асинхронный векторный рендеринг SVG. |
| BUG-045 | 2026-08-31 | Сетевая блокировка внешнего сервера plantuml.com делает визуализацию PlantUML недоступной | UI / MermaidViewer.tsx | Высокая | Исправлен | Реализован 100% автономный локальный (offline) векторный рендерер PlantUML-диаграмм с полноценной темной темой без каких-либо внешних сетевых запросов. |
| BUG-046 | 2026-08-31 | ER-диаграмма не строит модель данных в frontend-проектах без SQL/DTO, игнорируя JS/TS структуры | Engine / postgresParser.ts | Высокая | Исправлен | Реализован универсальный парсер `parseJavaScriptObjectStructures`, извлекающий сущности из констант, маппингов (например `nodesMap`), состояний и типов с автоматическим выводом типов полей и связей (1:N, N:M, tree). |
| BUG-047 | 2026-08-31 | В типах SET/ARRAY не указываются типы элементов, в таблице сущности не отображается целевая таблица для FK | Engine & UI / postgresParser.ts, DataModelView.tsx | Средняя | Исправлен | Реализована строгая типизация элементов коллекций (`INTEGER ARRAY`, `FOLDER SET`, `CATEGORY SET`), а в таблице сущности добавлено отображение и интерактивный переход к целевой таблице для внешних ключей (`UUID → users(id)`, `FK → FolderNode.id`). |
| BUG-048 | 2026-08-31 | В названиях элементов экранных форм не выводится описание (title/label/комментарий) перед именем класса | Engine / flowTracer.ts, DataFlowsView.tsx | Средняя | Исправлен | Реализован алгоритм `extractElementMetadata`, выводящий сначала описание (title/label/aria-label/comment/placeholder), а затем имя класса или тип компонента (например: `Уменьшить (Zoom Out) (p-1 hover:bg-gray-800...)`). |
| BUG-049 | 2026-08-31 | Неявные связи по типам сущностей (`run: AnalysisRun`, `tree: FileNode`, `endpoints: ApiEndpoint[]`) не связываются в ERD, отсутствуют описания атрибутов | Engine & UI / postgresParser.ts, DataModelView.tsx | Средняя | Исправлен | Реализовано автоматическое связывание атрибутов, ссылающихся на другие сущности (1:1 и 1:N), извлечение JSDoc/комментариев полей и добавление колонки «Описание» в таблицу и Mermaid ERD. |
| BUG-050 | 2026-08-31 | В таблице сущностей и инспекторе ER диаграммы отсутствует переход к исходному коду объявления сущности и колонок | Engine & UI / postgresParser.ts, DataModelView.tsx, App.tsx | Средняя | Исправлен | Реализована фиксация путей (`sourceFile`) и номеров строк (`sourceLine`) для всех найденных сущностей/колонок, добавлены кнопки навигации в списке таблиц, заголовке инспектора и строках атрибутов с переходом в Explorer. |
| BUG-051 | 2026-08-31 | Вкладка ER диаграммы не открывалась после анализа из-за ошибки инициализации графа `nodesData` | UI / DataModelView.tsx | Высокая | Исправлен | Восстановлено корректное вычисление `nodesData`/`linksData` и `diagramMode` в хуках `DataModelView`, добавлен безопасный доступ `selectedEntity?.attributes` при рендере. |
| BUG-052 | 2026-08-31 | Таблица расшифровки сущностей и колонок ERD не отображалась из-за отсутствия скроллинга контейнера | UI / DataModelView.tsx | Средняя | Исправлен | В корневой контейнер `DataModelView.tsx` добавлен класс `overflow-y-auto h-full max-w-6xl mx-auto`, добавлен переключатель моделей (репозиторий / демо-модель) и безопасный поиск атрибутов. |

---

### [BUG-031] Структура Request не отображалась в карте API, отсутствие детального модального окна DTO ответа и перехода к коду
- **Дата обнаружения**: 2026-08-28
- **Компонент**: Engine & UI / ApiExtractor, ApiMapView, RepoExplorerView, App.tsx
- **Критичность**: Высокая
- **Статус**: Исправлен
- **Шаги воспроизведения**:
  1. Запустить анализ репозитория со спецификацией Swagger/OpenAPI или ASP.NET/.NET/FastAPI контроллерами.
  2. Открыть страницу «Карта API» и развернуть метод с Request Body или ответом 200.
  3. Обратить внимание: структура Request Body не выводилась (писало «Тело запроса не требуется»), при 200 ответе выводился статический текст «JSON payload», а клик по исходному коду не выполнял переход.
- **Ожидаемое поведение**:
  - Фрейм «Исходный код» кликабелен и переходит в «Дерево проекта» с подсветкой строки и кнопкой «Назад к API».
  - Вместо статического текста «JSON payload» пишется имя DTO (или типа возврата `int`, `int[]`), при клике открывается модальное окно с таблицей полей (параметр, тип, описание) и примером JSON.
  - Структура Request Body извлекается из `$ref` компонентов схем/параметров и выводится в виде таблицы полей и JSON примера.
- **Фактическое поведение**: Структура Request Body не отображалась из-за отсутствия резолвинга `$ref` и несовпадения интерфейса; схема ответов содержала только статическую надпись.
- **Причина и решение**:
  - В `ApiExtractor` добавлен резолвинг схем `$ref` для OpenAPI 3.x и Swagger 2.0 (включая параметры `in: "body"`), извлечение массивов и скалярных типов (`int`, `long`, `string`, `boolean`, `UUID`).
  - В `ApiMapView` реализовано модальное окно `ResponseDtoModal` с поддержкой скалярных значений (`int`), массивов (`int[]`) и DTO моделей, а также кликабельный исходный код с `onNavigateToSource`.
  - В `RepoExplorerView` внедрена поддержка `focusedSource` (авто-раскрытие папок, подсветка строки и плавный скролл) и кнопка «← Назад к API».

### [BUG-035] Вкладка «Репозитории» перестала открываться из-за ReferenceError
- **Дата обнаружения**: 2026-08-28
- **Компонент**: UI / RepositoriesView.tsx
- **Критичность**: Высокая
- **Статус**: Исправлен
- **Шаги воспроизведения**:
  1. Запустить приложение и кликнуть на вкладку «Репозитории» в боковом меню.
- **Ожидаемое поведение**: Отображение списка проектов и репозиториев с кнопкой выбора локальной папки.
- **Фактическое поведение**: Вкладка не открывается из-за сбоя в цикле рендеринга React.
- **Причина и решение**:
  - В интерфейс `RepositoriesViewProps` было добавлено поле `onOpenLocalFolder?: () => void`, и внутри JSX тулбара добавлена проверка `{onOpenLocalFolder && ...}`.
  - Однако в деструктуризации аргументов функции `const RepositoriesView: React.FC<RepositoriesViewProps> = ({ repos, ... })` параметр `onOpenLocalFolder` был опущен, что вызывало `ReferenceError: onOpenLocalFolder is not defined`.
  - Добавлено явное извлечение `onOpenLocalFolder` в аргументах компонента.

### [BUG-036] Ссылки на DTO/API в локальном репозитории вели на .md файлы документации вместо кода методов
- **Дата обнаружения**: 2026-08-28
- **Компонент**: Engine & UI / RepoExplorerView, ApiExtractor, FlowTracer
- **Критичность**: Высокая
- **Статус**: Исправлен
- **Шаги воспроизведения**:
  1. Открыть локальную папку проекта или репозиторий с файлами документации (`README.md`, `docs/api/*.md`).
  2. В карте API или в структуре экранной формы нажать на DTO или «Исходный код» метода.
- **Ожидаемое поведение**: Открытие файла исходного кода (`.ts`, `.cs`, `.py`, `.java`, `.go`) с объявлением интерфейса, класса или метода контроллера.
- **Фактическое поведение**: Открывался `.md` файл документации из-за текстового вхождения названия DTO/метода.
- **Причина и решение**:
  - В `RepoExplorerView.resolveTargetNode` введена строгая пенализация (-60 баллов) для не-кодовых файлов (`.md`, `.txt`, `.doc`) и бонус (+15) для кодовых файлов (`.ts`, `.cs`, `.py`, `.go` и т.д.). Поиск по содержимому ограничен синтаксическими декларациями (`interface`, `class`, `type`, `struct`, `record`, `def`).
  - В `ApiExtractor` добавлен метод `findImplementingSourceCode`, связывающий OpenAPI/Swagger маршруты с реальными контроллерами в кодовой базе.
  - В `FlowTracer.resolveDtoFilePath` исключены `.md` файлы.

### [BUG-037] ER диаграмма при анализе репозитория не изменялась и показывала тестовые банковские таблицы
- **Дата обнаружения**: 2026-08-28
- **Компонент**: Engine & UI / PostgresParser, DataModelView
- **Критичность**: Высокая
- **Статус**: Исправлен
- **Шаги воспроизведения**:
  1. Запустить анализ проекта без прямых `.sql` файлов (например, проекта на Prisma, Python SQLAlchemy, C# EF Core или TypeScript).
  2. Открыть вкладку «Модель данных / ERD».
- **Ожидаемое поведение**: Построение ER-диаграммы на основе реальных моделей анализируемого репозитория.
- **Фактическое поведение**: Отображались тестовые таблицы банковского шлюза (`roles`, `users`, `accounts`, `categories`, `products`, `orders`).
- **Причина и решение**:
  - В `PostgresParser` реализован многоуровневый экстрактор моделей: SQL DDL, Prisma schemas, Python SQLAlchemy/Django, .NET C# EF Core, TypeScript entities и API DTOs.
  - Удалена статическая подстановка банковских таблиц при реальном анализе.
  - В `DataModelView` исправлен хук синхронизации `useSampleModel`, который теперь сбрасывается в `false` при получении сущностей из реального репозитория.

### [BUG-038] На панели управления (Dashboard) не отображаются результаты анализа экранных форм
- **Дата обнаружения**: 2026-08-31
- **Компонент**: UI / DashboardView.tsx
- **Критичность**: Средняя
- **Статус**: Исправлен
- **Шаги воспроизведения**:
  1. Выбрать локальный репозиторий с фронтенд-компонентами (React, Vue, HTML и т.д.) и запустить анализ.
  2. Перейти на Панель управления (Dashboard).
- **Ожидаемое поведение**: Отображение карточки метрики экранных форм и блока со списком обнаруженных экранных форм и элементов.
- **Фактическое поведение**: На Dashboard экранные формы отсутствовали, хотя во вкладке «Data Flows» они были.
- **Причина и решение**: В `DashboardView` добавлена карточка метрики «Экранных форм UI» (`screenForms.length`) и обзорный интерактивный блок «Экранные формы и элементы интерфейса» со списком обнаруженных компонентов, маршрутов, элементов и кнопкой перехода в Data Flows & Sequence.

### [BUG-039] Клик по DTO элемента экранной формы открывает .gitignore из-за отсутствия файла и fallback
- **Дата обнаружения**: 2026-08-31
- **Компонент**: UI & Engine / RepoExplorerView, FlowTracer
- **Критичность**: Высокая
- **Статус**: Исправлен
- **Шаги воспроизведения**:
  1. В разделе Data flows развернуть спойлер элемента экранной формы.
  2. Нажать на ссылку DTO.
- **Ожидаемое поведение**: Переход к реальному объявлению элемента или его обработчика в файле компонента.
- **Фактическое поведение**: Открывался файл `.gitignore`, так как выдуманный DTO-файл не существует, и резолвер падал в fallback `allFiles[0]`.
- **Причина и решение**: Вымышленные DTO заменены на кликабельную ссылку на исходное место объявления элемента (`file.jsx:line`) и код элемента. В `RepoExplorerView.resolveTargetNode` устранен безусловный fallback на первый файл `.gitignore`.

### [BUG-040] Имя метода и DTO в экранных формах выдумывались парсером вместо извлечения реальных функций из JSX/HTML
- **Дата обнаружения**: 2026-08-31
- **Компонент**: Engine / FlowTracer.ts
- **Критичность**: Высокая
- **Статус**: Исправлен
- **Шаги воспроизведения**:
  1. Проанализировать компонент (например, `ConflictZone.jsx`).
  2. Открыть экранную форму в Data flows.
- **Ожидаемое поведение**: Имя метода содержит имя реальной функции-обработчика события (`onClick`, `onChange`, `onSubmit`, `useEffect`). Вместо вымышленного DTO отображается код объявления элемента.
- **Фактическое поведение**: Выводились сгенерированные заглушки `CatalogController.GetInitialDictionaries` и `ConflictZoneInitPayloadDTO`.
- **Причина и решение**: В `FlowTracer.extractScreenForms` реализован построчный парсинг AST/JSX/HTML/Vue: извлекаются реальные имена функций-обработчиков (`onClick={handleResolveConflict}` -> `handleResolveConflict()`, `useEffect` -> вызванная функция загрузки), точные номера строк и фрагменты кода объявления (`codeSnippet`).

### [BUG-041] В блоке Frontend Payload отображается фейковая заглушка даже при отсутствии параметров
- **Дата обнаружения**: 2026-08-31
- **Компонент**: Engine & UI / FlowTracer.ts, DataFlowsView.tsx, ScreenFormStructureModal.tsx
- **Критичность**: Средняя
- **Статус**: Исправлен
- **Шаги воспроизведения**:
  1. Развернуть элемент экранной формы в Data Flows.
- **Ожидаемое поведение**: Если реальный пэйлоад в коде отсутствует, блок «Frontend Payload» не должен отображаться.
- **Фактическое поведение**: Выводился сгенерированный мок JSON `{ trigger: "componentDidMount...", screenId: ..., cacheStrategy: ... }`.
- **Причина и решение**: Удалена генерация фиктивных JSON заглушек в `FlowTracer`. В `DataFlowsView.tsx` и `ScreenFormStructureModal.tsx` рендеринг блока «Frontend Payload» сделан строго условным (`elem.frontendPayload && Object.keys(elem.frontendPayload).length > 0`).

### [BUG-042] Лишние кнопки в верхней шапке элемента экранной формы над диаграммой
- **Дата обнаружения**: 2026-08-31
- **Компонент**: UI / DataFlowsView.tsx
- **Критичность**: Низкая
- **Статус**: Исправлен
- **Шаги воспроизведения**:
  1. Выбрать форму и элемент в разделе Data Flows.
  2. Обратить внимание на тулбар над Sequence-диаграммой.
- **Ожидаемое поведение**: В тулбаре присутствуют название формы, элемента, маршрут и единственная кнопка «Показать структуру».
- **Фактическое поведение**: В тулбаре дублировались кнопки перехода к исходному коду компонента, метода, строки и DTO, перегружая интерфейс.
- **Причина и решение**: Удалены лишние кнопки из тулбара `Selected Element Header Bar` в `DataFlowsView.tsx`, оставлены текстовые бейджи и кнопка «Показать структуру». Все детальные ссылки для перехода к коду доступны в левом аккордеоне элементов.

### [BUG-043] В легенде D3 структуры формы присутствуют отсутствующие на схеме элементы, отсутствует «Событие», «Backend методы» не переименованы
- **Дата обнаружения**: 2026-08-31
- **Компонент**: UI / ScreenFormStructureModal.tsx
- **Критичность**: Средняя
- **Статус**: Исправлен
- **Шаги воспроизведения**:
  1. Открыть структуру экранной формы для чистого frontend-компонента без БД и DTO.
  2. Обратить внимание на верхние заголовки слоев и легенду цветов.
- **Ожидаемое поведение**: Вверху выводятся только те слои, которые есть на графе (с правильной нумерацией 1..N), «Backend методы» переименованы в «Методы», в легенде присутствует «Событие (onLoad)», мок БД отсутствует.
- **Фактическое поведение**: Вверху статически выводились все 5 слоев включая отсутствующие DTO и Базу данных, «Backend методы» назывались статически, в легенде отсутствовало «Событие», а на схеме безусловно рисовалась PostgreSQL DB.
- **Причина и решение**: В `ScreenFormStructureModal.tsx` внедрен динамический расчет активных слоев (`activeLayersInfo`), верхняя полоса отображает только существующие слои с динамической нумерацией, D3 X-координаты центрируются по реальным колонкам, «Backend методы» заменены на «Методы», добавлено «Событие (onLoad)» в легенду, исключено безусловное добавление ноды базы данных при отсутствии SQL/DB связей.

### [BUG-044] Ошибка визуализации PlantUML и светлая тема диаграммы
- **Дата обнаружения**: 2026-08-31
- **Компонент**: Engine & UI / MermaidViewer.tsx, FlowTracer.ts, DataFlowsView.tsx
- **Критичность**: Высокая
- **Статус**: Исправлен
- **Шаги воспроизведения**:
  1. В разделе Data Flows переключить режим отображения диаграммы на «PlantUML Визуализация».
- **Ожидаемое поведение**: Отображение темной стилизованной Sequence-диаграммы PlantUML с поддержкой зума и панорамирования.
- **Фактическое поведение**: Диаграмма не отображалась из-за синтаксической ошибки, вызванной повторной авторазбивкой `participant ... as ...` в `formatSequenceDiagram`, и использовала дефолтную белую тему.
- **Причина и решение**: Устранить повторное форматирование готового PlantUML-кода, внедрить темную тему skinparam (`#070A13`, `#0F172A`, `#10B981`, `#06B6D4`, `#6366F1`) и асинхронную загрузку SVG с векторным рендерингом.

### [BUG-045] Сетевая блокировка внешнего сервера plantuml.com делает визуализацию PlantUML недоступной
- **Дата обнаружения**: 2026-08-31
- **Компонент**: UI / MermaidViewer.tsx
- **Критичность**: Высокая
- **Статус**: Исправлен
- **Шаги воспроизведения**:
  1. Открыть раздел Data Flows в режиме оффлайн или в корпоративной сети / РФ без VPN (где домен `plantuml.com` недоступен).
  2. Выбрать форму / элемент и переключиться на вкладку «PlantUML Визуализация».
- **Ожидаемое поведение**: Диаграмма отрисовывается мгновенно, автономно и локально, не требуя доступа к внешним сайтам.
- **Фактическое поведение**: Появляется ошибка «Не удалось загрузить визуализацию PlantUML» из-за недоступности `plantuml.com`.
- **Причина и решение**: Перевести визуализацию PlantUML на автономный локальный (клиентский) рендеринг: парсинг и компиляция PlantUML синтаксиса в векторный SVG локальным движком с кастомной темной палитрой PlantUML (`#070A13`, изумрудные акторы, циановые базы данных, индиго участники, голубые стрелки), при этом во вкладке «PlantUML Исходный код» оставлять чистый PlantUML код для экспорта в IDE.

### [BUG-046] ER-диаграмма не строит модель данных в frontend-проектах без SQL/DTO, игнорируя JS/TS структуры
- **Дата обнаружения**: 2026-08-31
- **Компонент**: Engine / postgresParser.ts, types.ts
- **Критичность**: Высокая
- **Статус**: Исправлен
- **Шаги воспроизведения**:
  1. Открыть и проанализировать чисто фронтендовый проект без миграций БД или Swagger API DTO.
  2. Перейти во вкладку «Модель данных (ERD)».
- **Ожидаемое поведение**: Приложение извлекает структуры данных из JS/TS объектов, констант, маппингов (`nodesMap`), структур графов и начальных состояний, строит сущности с атрибутами и связывает их между собой через атрибуты (FK, id, списки дочерних элементов).
- **Фактическое поведение**: ER-модель не строилась, оставаясь пустой или показывая синтетический сэмпл.
- **Причина и решение**: Реализовать в `PostgresParser` специализированный парсер `parseJavaScriptObjectStructures`, распознающий объектные константы, маппинги, графовые узлы и структуры данных в JS/TS коде с типизацией полей и связыванием по ключевым атрибутам (`id`, `parentId`, `folderId`, `categories`, `files`).

### [BUG-047] В типах SET/ARRAY не указываются типы элементов, в таблице сущности не отображается целевая таблица для FK
- **Дата обнаружения**: 2026-08-31
- **Компонент**: Engine & UI / postgresParser.ts, DataModelView.tsx
- **Критичность**: Средняя
- **Статус**: Исправлен
- **Шаги воспроизведения**:
  1. Открыть вкладку «Модель данных (ERD)» и выбрать сущность со связями или полями коллекций (`categories: new Set()`, `files: []`, `userId: ...`).
  2. Обратить внимание на типы `SET`, `ARRAY` — они отображались обобщенно без указания типа элементов, а в строках внешних ключей (`FK`) не отображалось имя таблицы/сущности, к которой ведет ключ.
- **Ожидаемое поведение**: Поля коллекций отображают конкретный тип элементов (например, `INTEGER ARRAY`, `FOLDER SET`, `FILE ARRAY`, `VARCHAR ARRAY`), а строки внешних ключей (`FK`) явно показывают целевую таблицу, на которую ссылается ключ (например, `UUID → users(id)` или `FK → FolderNode.id`).
- **Причина и решение**: Расширить типизацию коллекций в `postgresParser.ts` (вывод типа элементов по названию, элементам и связанным ERD-сущностям), а в `DataModelView.tsx` добавить отображение целевой таблицы в столбцах типа и ключа таблицы сущности.

### [BUG-048] В названиях элементов экранных форм не выводится описание (title/label/комментарий) перед именем класса
- **Дата обнаружения**: 2026-08-31
- **Компонент**: Engine / flowTracer.ts, DataFlowsView.tsx
- **Критичность**: Средняя
- **Статус**: Исправлен
- **Шаги воспроизведения**:
  1. Открыть раздел Data flows для экранных форм.
  2. Проверить наименования элементов: для кнопок, имеющих атрибуты `title`, `aria-label`, `label` или комментарии (например, `<button className="p-1 hover:bg-gray-800 text-gray-300 rounded transition" title="Уменьшить (Zoom Out)">`), выводилось лишь имя класса или заглушка без описания.
- **Ожидаемое поведение**: Сначала выводить описание (title, label, description или комментарий), если найдено, а затем имя класса или тип компонента (например, `Уменьшить (Zoom Out) (p-1 hover:bg-gray-800...)`).
- **Причина и решение**: Реализовать в `flowTracer.ts` метод `extractElementMetadata`, извлекающий атрибуты описания (`title`, `aria-label`, `label`, `placeholder`, предшествующий комментарий `// ...`) и класс (`className`), формируя приоритетное составное имя «Описание (Имя класса/тег)».

### [BUG-049] Неявные связи по типам сущностей (`run: AnalysisRun`, `tree: FileNode`, `endpoints: ApiEndpoint[]`) не связываются в ERD, отсутствуют описания атрибутов
- **Дата обнаружения**: 2026-08-31
- **Компонент**: Engine & UI / postgresParser.ts, DataModelView.tsx
- **Критичность**: Средняя
- **Статус**: Исправлен
- **Шаги воспроизведения**:
  1. Открыть модель данных ERD для сущности, содержащей поля со сложными типами других моделей (например `FullAnalysisResult` с полями `run: AnalysisRun`, `tree: FileNode`, `endpoints: ApiEndpoint[]`).
  2. Обратить внимание: поля `run` и `tree` не помечались как FK и не связывались линиями на диаграмме с `AnalysisRun` и `FileNode`, а также отсутствовала колонка и отображение описаний атрибутов.
- **Ожидаемое поведение**: Поля, типы которых ссылаются на другие сущности (как одиночные `ANALYSISRUN`, `FILENODE`, так и коллекции `APIENDPOINT ARRAY`), помечаются как FK со связями на D3 графе и Mermaid ERD; в таблице сущности и диаграмме выводятся описания атрибутов, найденные в коде (JSDoc, комментарии, SQL comments).
- **Причина и решение**: В `postgresParser.ts` добавить прямую резолюцию типов по списку сущностей в `inferImplicitRelationships`, реализовать парсинг JSDoc/комментариев полей, в `DataModelView.tsx` добавить колонку «Описание» и отображение комментариев в Mermaid/D3.

### [BUG-050] В таблице сущностей и инспекторе ER диаграммы отсутствует переход к исходному коду объявления сущности и колонок
- **Дата обнаружения**: 2026-08-31
- **Компонент**: Engine & UI / postgresParser.ts, DataModelView.tsx, App.tsx
- **Критичность**: Средняя
- **Статус**: Исправлен
- **Шаги воспроизведения**:
  1. Открыть вкладку ER диаграммы и выбрать сущность из списка таблиц или на холсте.
  2. В заголовке таблицы и в списке отсутствовала возможность открыть файл и перейти к строке объявления сущности в репозитории.
- **Ожидаемое поведение**: В списке таблиц и в блоке выбранной сущности выводить путь к исходному файлу и строке с кнопкой быстрого перехода в просмотрщик репозитория с возможностью вернуться обратно.
- **Причина и решение**: Добавить поля `sourceFile` и `sourceLine` в интерфейсы `EntityModel` и `EntityAttribute`, сохранять пути и строки при парсинге DDL, Prisma, Python, .NET, TypeScript и JavaScript структур, в `DataModelView.tsx` добавить интерактивные бейджи и иконки перехода к коду, а в `App.tsx` пробросить обработчик `handleNavigateToSource`.

### [BUG-051] Вкладка ER диаграммы не открывалась после анализа из-за ошибки инициализации графа `nodesData`
- **Дата обнаружения**: 2026-08-31
- **Компонент**: UI / DataModelView.tsx
- **Критичность**: Высокая
- **Статус**: Исправлен
- **Шаги воспроизведения**:
  1. Запустить анализ репозитория и после завершения анализа переключиться на вкладку «Модель данных & ERD».
  2. Вкладка не открывалась из-за ошибки `ReferenceError: nodesData is not defined` в теле хука `useEffect`.
- **Ожидаемое поведение**: Вкладка должна мгновенно открываться, отображая интерактивный граф D3 и детальный инспектор выбранной таблицы.
- **Причина и решение**: В `DataModelView.tsx` восстановлены объявления `diagramMode`, `nodesData` и `linksData` в `useMemo`, добавлены проверки на `selectedEntity?.attributes` и безопасное обращение к полям сущности.

| BUG-053 | 2026-08-31 | Строковые массивы классифицировались как ARRAY вместо ENUM, отсутствовали системные типы в ERD и источники классификации | Engine & UI / postgresParser.ts, DataModelView.tsx | Средняя | Исправлен | Строковые массивы выделяются в отдельные ENUM сущности с FK-связями; параметры системных типов регистрируются как отдельные сущности; для всех сущностей добавлены бейджи источников классификации. |
| BUG-054 | 2026-09-01 | ENUM в табличном представлении выводился с псевдо-реляционными колонками (VARCHAR, PK, Nullable, val_1) вместо чистого списка значений | UI / DataModelView.tsx | Средняя | Исправлен | Для ENUM сущностей реализован специализированный инспектор: заголовок/ключ ENUM, описание, карточки значений и чистая таблица спецификации (#, Литерал, Описание, Статус) без колонок реляционных БД. |
| BUG-055 | 2026-09-01 | JS/TS парсер не считывал ENUM из union-комментариев (`status: 'IDLE' // 'IDLE'|'RUNNING'`), структур массивов (`log`, `errors`) и многострочных литералов (`capabilities`) | Engine / postgresParser.ts | Высокая | Исправлен | Внедрен токенизатор с учетом скобочной вложенности (bracket/brace-aware), парсинг строковых объединений из комментариев, извлечение подструктур `Array of { ... }` и `Array of { file, error }` с автогенерацией ENUM-типов и связей. |
| BUG-056 | 2026-09-01 | Утечка однострочного комментария после запятой к следующему свойству объекта и перехват типа объединения над структурой | Engine / postgresParser.ts | Высокая | Исправлен | В `parseObjectLiteralProperties` добавлен разбор inline-комментария на той же строке после запятой; в `extractAttributesFromObjectLiteral` правило парсинга структуры объекта `Array of { ... }` перемещено перед общим правилом union-комментариев. |
| BUG-057 | 2026-09-01 | PlantUML визуализация и генерация кода в ER модели не работали из-за отсутствия конвертера ERD синтаксиса | Engine & UI / postgresParser.ts, MermaidViewer.tsx | Высокая | Исправлен | Добавлен метод `generatePlantUmlErd` в `postgresParser.ts`, поле `erDiagramPlantUml` в `DataModel`, а в `MermaidViewer.tsx` реализована двусторонняя конвертация Mermaid `erDiagram` $\leftrightarrow$ PlantUML ERD (`entity ...`, `enum ...`). |

---

### [BUG-053] Строковые массивы классифицировались как ARRAY вместо ENUM, отсутствовали системные типы в ERD и источники классификации
- **Дата обнаружения**: 2026-08-31
- **Компонент**: Engine & UI / postgresParser.ts, DataModelView.tsx, types.ts
- **Критичность**: Средняя
- **Статус**: Исправлен
- **Шаги воспроизведения**:
  1. В коде проекта объявить массив строк настроек, например: `capabilities: ['Автоматическая классификация...', 'Возможность исключать...']`.
  2. При анализе свойство классифицировалось как `CAPABILITY ARRAY` без выделения в ENUM сущность.
  3. Параметры, ссылающиеся на системные типы (`file`, `folder`, `system`), не включались в модель данных.
  4. Для сущностей не выводился способ их классификации (источник: DTO, SQL DDL, Prisma, JS/TS структура, ENUM, системный тип).
- **Ожидаемое поведение**: Выделять массивы строковых литералов в отдельные ENUM сущности с описаниями и связями `1:N`; регистрировать системные типы как отдельные сущности; отображать бейджи источников классификации.
- **Причина и решение**:
  1. В `types.ts` и `postgresParser.ts` реализовать автогенерацию ENUM сущностей из массивов строк и TS enum, регистрацию системных типов (`File`, `Folder`, `System`, `FileNode`) и классификацию `sourceType` / `sourceLabel`.
  2. В `DataModelView.tsx` внедрить быстрое фильтрование по типам (Все, Таблицы, ENUM, Системные), бейджи источников и блок значений перечислений.

---

### [BUG-054] ENUM в табличном представлении выводился с псевдо-реляционными колонками (VARCHAR, PK, Nullable, val_1) вместо чистого списка значений
- **Дата обнаружения**: 2026-09-01
- **Компонент**: UI / DataModelView.tsx
- **Критичность**: Средняя
- **Статус**: Исправлен
- **Шаги воспроизведения**:
  1. Выбрать в списке сущностей модель с `isEnum: true` (например, `CapabilityEnum`, `StatusEnum`).
  2. В детальном представлении отображалась стандартная реляционная таблица со столбцами: «Колонка / Свойство», «Тип данных: VARCHAR», «Ключ: PK», «Nullable: NOT NULL», «val_1».
- **Ожидаемое поведение**: ENUM не должен иметь псевдо-реляционных колонок БД. Ключом является само наименование ENUM, а состав должен представлять собой чистый список допустимых значений с карточками/бейджами и таблицей литералов.
- **Причина и решение**:
  В `DataModelView.tsx` реализовано раздельное отображение: для ENUM выводится карточка с именем-ключом, бейджи значений, спецификация значений (#, Литерал, Описание, Статус) и список сущностей, использующих данный ENUM, без лишних реляционных колонок.

---

### [BUG-055] JS/TS парсер не считывал ENUM из union-комментариев (`status: 'IDLE' // 'IDLE'|'RUNNING'`), структур массивов (`log`, `errors`) и многострочных литералов (`capabilities`)
- **Дата обнаружения**: 2026-09-01
- **Компонент**: Engine / postgresParser.ts
- **Критичность**: Высокая
- **Статус**: Исправлен
- **Шаги воспроизведения**:
  1. В коде объявить `status: 'IDLE', // 'IDLE' | 'RUNNING' | 'PAUSED' | 'DONE' | 'STOPPED'`. Свойство оставалось строкой `VARCHAR` без ENUM.
  2. В коде объявить `log: [], // Array of { timestamp, type: 'info'|'success'|'error'|'warning', text }`. Массив оставался пустым `OBJECT ARRAY`.
  3. В `TOOLS_CONFIG` массив `capabilities: [ '...', '...' ]` был разбит на несколько строк — построчный парсер терял элементы массива из-за отсутствия закрывающей скобки на первой строке.
- **Ожидаемое поведение**: Автоматически распознавать union-комментарии и создавать ENUM сущности; извлекать дочерние структуры `LogItem`, `ErrorItem` с подтипами перечислений; считывать многострочные массивы строковых литералов.
- **Причина и решение**:
  В `postgresParser.ts` разработан метод `parseObjectLiteralProperties` со скобочно-сбалансированным разбором литералов, алгоритмом детекции объединений строковых литералов в комментариях и парсером структур `Array of { ... }`.

---

### [BUG-056] Утечка однострочного комментария после запятой к следующему свойству объекта и перехват типа объединения над структурой
- **Дата обнаружения**: 2026-09-01
- **Компонент**: Engine / postgresParser.ts
- **Критичность**: Высокая
- **Статус**: Исправлен
- **Шаги воспроизведения**:
  1. В объекте JS объявить подряд свойства с комментариями после запятой:
     ```javascript
     log: [],        // Array of { timestamp, type: 'info'|'success'|'error'|'warning', text }
     errors: [],     // Array of { file, error }
     ```
  2. Из-за завершения разбора на запятой перед комментарием `log` терял свой комментарий, а `errors` ошибочно получал комментарий от `log`.
  3. Кроме того, общее правило union-комментариев срабатывало на `type: 'info'|...` внутри `{ ... }` и превращало весь `log` в ENUM.
- **Ожидаемое поведение**: Комментарий после запятой на той же строке должен относиться к текущему параметру; `log` должен формировать подсущность `LogItem` с подтипом `LogTypeEnum`, а `errors` — подсущность `ErrorItem`.
- **Причина и решение**:
  1. В `parseObjectLiteralProperties` добавлен поиск inline-комментария `// ...` на остатке текущей строки после запятой до переноса строки `\n`.
  2. В `extractAttributesFromObjectLiteral` правило детекции структуры объекта `structPattern` перемещено на первое место перед `unionPattern`.

---

| BUG-057 | 2026-09-01 | PlantUML визуализация и генерация кода в ER модели не работали из-за отсутствия конвертера ERD синтаксиса | Engine & UI / postgresParser.ts, MermaidViewer.tsx | Высокая | Исправлен | Реализован `generatePlantUmlErd` в `postgresParser.ts`, добавлена поддержка ERD синтаксиса в конвертерах `MermaidViewer.tsx`. |
| BUG-058 | 2026-09-01 | Сбой D3 графа структуры проектов при вводе монорепозиториев и версий репозиториев | UI & Backend / ProjectGraphView, bitbucketClient, main.ts | Высокая | Исправлен | Расширен `HierarchyNode` (типы `subproject`, `repoType`, `similarityWith`), реализован `buildHierarchyFromRepositories` с динамической сборкой подпроектов и версий, добавлены пунктирные эволюционные связи подобия в D3 Force Canvas, синхронизированы селекторы всех 4 представлений. |

---

### [BUG-058] Сбой D3 графа структуры проектов при вводе монорепозиториев и версий репозиториев
- **Дата обнаружения**: 2026-09-01
- **Компонент**: UI & Backend / ProjectGraphView.tsx, bitbucketClient.ts, main.ts, types.ts
- **Критичность**: Высокая
- **Статус**: Исправлен
- **Шаги воспроизведения**:
  1. Открыть вкладку «D3 граф проектов».
  2. Граф не отображался или холст оставался пустым из-за рассинхронизации селекторов и отсутствия поддержки узлов монорепозиториев.
  3. Не отображались монорепозитории с подпроектами, версии репозиториев с метриками сходства и локально открытые папки.
- **Ожидаемое поведение**:
  - Граф корректно отрисовывает все 4 представления: Bitbucket/Сервер $\rightarrow$ Проекты $\rightarrow$ Репозитории $\rightarrow$ Подпроекты/Ветки $\rightarrow$ Коммиты.
  - На графе отображаются узлы монорепозиториев, подпроектов, версионированных копий со связями подобия (similarity links).
  - Селекторы проектов, репозиториев и веток синхронизированы, боковая панель инспектора отображает детальную информацию и кнопку запуска анализа.
- **Причина и решение**:
  - В `types.ts` расширен интерфейс `HierarchyNode` (добавлен тип `subproject`, параметры `repoType`, `subprojects`, `similarityWith`, `isLocal`, `localPath`).
  - В `bitbucketClient.ts` реализован метод `buildHierarchyFromRepositories` и обновлен `generateMockHierarchy` для генерации узлов подпроектов монорепозиториев и эволюционных копий.
  - В `main.ts` метод `get-project-graph` переведен на генерацию дерева из сохраненных репозиториев `localStore.getRepositories()`.
  - В `ProjectGraphView.tsx` внедрена отрисовка подпроектов (cyan узлы `📦`), монорепозиториев (purple `🏗️`), версий (amber `🧬`) со специальными пунктирными связями подобия и процентом сходства. Обеспечена 100% синхронизация каскадных селекторов и надежная локальная демо-заглушка.
















